# F29 (vuelta 1) — Sub-G 915 MHz Presets Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Agregar 3 presets PROP_PRESETS Sub-G 915 MHz (sidewalk 50k, sidewalk 250k, wisun 50k) con tests unitarios + smoke marker test 30/30 OTA sobre 2 CatSniffer boards. Cero cambios firmware.

**Architecture:** Branch `feature/f29-stack-presets` desde `main` HEAD `f50c5e3`. Bundle único con 3 commits (presets+schema, F29-specific tests, smoke script). Después: pre-flight baseline `gfsk_868_50k`, smoke F29 10/10 × 3 presets, tag `v2.0-f29-partial`, FF a `main`. Total: ~5 commits + 1 tag.

**Tech Stack:** Python 3.11+ (pyserial, pytest), pre-commit (black, isort, flake8, mypy), CC1352 firmware ya validado (no se rebuilds), CatSniffer #1 (`/dev/ttyACM2`) + #2 (`/dev/ttyACM5` u otro disponible).

**Spec source:** `docs/superpowers/specs/2026-05-03-f29-subg-915-presets-design.md` (commit `f50c5e3`).

**Hardware:** 2 boards CatSniffer. TX desde board #2 (board #1 tiene Sub-1GHz TX hardware fault per `project_hardware`), RX desde board #1 (Sub-1GHz RX OK). Antena CatSniffer cubre 868 y 915 MHz sin issue (solo 433 MHz es marginal).

---

## Task 0: Crear working branch

**Files:**
- None (git operation)

- [ ] **Step 1: Verificar baseline en HEAD `f50c5e3`**

```bash
git status
git log --oneline -3
```

Expected: HEAD = `f50c5e3 plan(f29): vuelta 1 spec — 3 presets Sub-G 915 MHz Sidewalk + Wi-SUN`. Working tree limpio (los WIP de F8f ya cerraron en commits previos).

- [ ] **Step 2: Crear branch `feature/f29-stack-presets`**

```bash
git checkout -b feature/f29-stack-presets
git status
```

Expected: branch creada, working tree limpio.

---

## Task 1: Agregar 3 presets a PROP_PRESETS (TDD)

**Files:**
- Modify: `python/feralrf/presets.py` (agregar bloque después de la sección 868 MHz)
- Test: `python/tests/test_props.py` (agregar al final del archivo, TDD)

**Background:** El dict `PROP_PRESETS` tiene 16 presets validados (433 / 868 / 169 / 2440 MHz). Agregamos un bloque "902-928 MHz ISM Band". Cada preset es un dict con keys: `frequency_hz`, `mod_type`, `symbol_rate`, `deviation`, `rx_bw`, `sync_word`. El schema test existente parametrizado sobre `PROP_PRESETS.keys()` cubre automáticamente las nuevas entradas — no necesita cambios para validar shape básica.

- [ ] **Step 1: Escribir tests F29-specific (failing)**

Append al final de `python/tests/test_props.py` (después de la última función test):

```python
# F29 vuelta 1 — Sub-G 915 MHz presets


F29_PRESET_NAMES = ("sidewalk_915_fsk_50k", "sidewalk_915_fsk_250k", "wisun_915_fsk_50k")


@pytest.mark.parametrize("name", F29_PRESET_NAMES)
def test_f29_preset_present(name):
    """F29 vuelta 1 — los 3 presets nuevos están en PROP_PRESETS."""
    assert name in PROP_PRESETS, f"Preset {name} ausente"


@pytest.mark.parametrize("name", F29_PRESET_NAMES)
def test_f29_preset_in_915_band(name):
    """F29 vuelta 1 — frecuencia debe estar en banda 902-928 MHz US ISM."""
    f = PROP_PRESETS[name]["frequency_hz"]
    assert 902_000_000 <= f <= 928_000_000, f"{name}: freq {f} fuera de banda 902-928 MHz"


@pytest.mark.parametrize("name", F29_PRESET_NAMES)
def test_f29_preset_uses_fsk(name):
    """F29 vuelta 1 — usa FSK puro (mod_type=0). GFSK fallback queda para F29.b."""
    assert PROP_PRESETS[name]["mod_type"] == 0, f"{name}: mod_type debe ser 0 (FSK)"


def test_f29_preset_count():
    """F29 vuelta 1 — exactamente 3 presets nuevos en banda 915."""
    f29 = [n for n in PROP_PRESETS if n in F29_PRESET_NAMES]
    assert len(f29) == 3, f"Esperaba 3 F29 presets, encontré {len(f29)}: {f29}"
```

- [ ] **Step 2: Verificar que los tests fallan**

```bash
cd python && PYTHONPATH=. pytest tests/test_props.py::test_f29_preset_present -v 2>&1 | tail -10
```

Expected: 3 FAIL (uno por parametrize value) — `KeyError` o `assert` por preset ausente.

- [ ] **Step 3: Editar `python/feralrf/presets.py`**

Buscar la línea que cierra el bloque 868 MHz (después de `"ook_868_4k8"` o similar) y agregar el bloque nuevo justo antes del cierre del dict (`}` final). Localizar el final del dict y agregar:

```python
    # === 902-928 MHz ISM Band (US / Sidewalk / Wi-SUN FAN 1.0) ===
    "sidewalk_915_fsk_50k": dict(
        frequency_hz=915000000,
        mod_type=0,  # FSK
        symbol_rate=50000,
        deviation=100,  # template GFSK 868 ya validado
        rx_bw=0x52,
        sync_word=0x930B51DE,
    ),
    "sidewalk_915_fsk_250k": dict(
        frequency_hz=915000000,
        mod_type=0,  # FSK
        symbol_rate=250000,
        deviation=200,  # escalada vs 50k variant
        rx_bw=0x56,  # bandwidth más ancho para 250k
        sync_word=0x930B51DE,
    ),
    "wisun_915_fsk_50k": dict(
        frequency_hz=902200000,  # Wi-SUN FAN 1.0 NA-1 plan canal 0
        mod_type=0,  # FSK
        symbol_rate=50000,
        deviation=100,
        rx_bw=0x52,
        sync_word=0x930B51DE,
    ),
```

Antes de aplicar el edit, lee el archivo para encontrar la inserción correcta:

```bash
grep -n "PROP_PRESETS = {\|^}" python/feralrf/presets.py
```

Insertar las nuevas entradas ANTES del `}` que cierra el dict.

- [ ] **Step 4: Verificar que los tests pasan**

```bash
cd python && PYTHONPATH=. pytest tests/test_props.py -v 2>&1 | tail -20
```

Expected: TODOS los tests pass — los 12 nuevos (4 funciones × 3 parametrize) + los pre-existentes con 19 presets en lugar de 16 (parametrize del schema cubre los nuevos automáticamente).

- [ ] **Step 5: Suite completa Python (regression check)**

```bash
cd python && PYTHONPATH=. pytest -q
```

Expected: ≥ 466 pass (era 454 antes de F29) y 5 skipped, 0 failed. (12 nuevos tests F29 + 6 nuevos parametrize hits del schema test sobre los 3 presets nuevos × ~6 parametrize tests existentes.)

- [ ] **Step 6: Pre-commit + commit**

```bash
cd /home/sabas/Documents/electroniccats/FeralRF
pre-commit run --files python/feralrf/presets.py python/tests/test_props.py
git add python/feralrf/presets.py python/tests/test_props.py
git commit -m "$(cat <<'EOF'
feat(f29): add 3 Sub-G 915 MHz presets — Sidewalk + Wi-SUN baseline

Add sidewalk_915_fsk_50k (915 MHz / 50k symbol rate), sidewalk_915_fsk_250k
(915 MHz / 250k), and wisun_915_fsk_50k (902.2 MHz / 50k, Wi-SUN FAN 1.0
NA-1 channel 0) to PROP_PRESETS. Fidelidad media: banda y symbol rate del
estándar real, deviation/rx_bw heredados del template GFSK 868 ya validado,
sync word genérico. mod_type=0 (FSK puro).

Add 4 F29-specific test functions (parametrized over the 3 names): present
in dict, frequency in 902-928 MHz band, mod_type==0, count exactly 3. The
existing schema parametrize tests automatically cover the new presets.

Cero cambios firmware. Smoke marker test queda para Task 5 del plan.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

Expected: pre-commit clean (puede haber reformat black/isort de los nuevos dict entries — aceptar), commit lands.

---

## Task 2: Crear smoke `smoke_f29_subg_915.py`

**Files:**
- Create: `python/examples/smoke_f29_subg_915.py`

**Background:** El smoke F29 envuelve a `smoke_ota_txrx.py` (que ya soporta `--preset`) y lo corre 3 veces, una por preset. Cada corrida: TX board envía 10 markers, RX board cuenta. Pass = ≥ 10 markers cada uno (30/30 total).

Patrón establecido en `smoke_ota_txrx.py:40-129`: argparse `--tx-port` `--rx-port` `--preset` `--count`. Lo extendemos a multi-preset en un script wrapper que invoca la lógica equivalente in-process (no fork) para mantener Radio sessions limpias.

- [ ] **Step 1: Crear `python/examples/smoke_f29_subg_915.py`**

```python
#!/usr/bin/env python3
"""F29 vuelta 1 — Sub-G 915 MHz preset smoke (Sidewalk + Wi-SUN baseline).

Recorre los 3 presets F29 (sidewalk_915_fsk_50k, sidewalk_915_fsk_250k,
wisun_915_fsk_50k) y para cada uno: TX board envía N markers OTA, RX board
cuenta. Pass total = (count × 3) markers; per-preset pass = >= count markers.

Usage:
    python smoke_f29_subg_915.py --tx-port /dev/ttyACM5 --rx-port /dev/ttyACM2
    python smoke_f29_subg_915.py --tx-port /dev/ttyACM5 --rx-port /dev/ttyACM2 --count 10
"""

import argparse
import re
import sys
import time

import serial


F29_PRESETS = ("sidewalk_915_fsk_50k", "sidewalk_915_fsk_250k", "wisun_915_fsk_50k")


def reset_cc1352(port: str) -> None:
    """Reset CC1352 via RP2040 shell port (data port + 2)."""
    m = re.search(r"(\d+)$", port)
    if not m:
        return
    shell = port[: m.start(1)] + str(int(m.group(1)) + 2)
    try:
        s = serial.Serial(shell, 115200, timeout=1.0, write_timeout=1.0)
        s.write(b"boot\r\n")
        time.sleep(0.5)
        s.write(b"exit\r\n")
        time.sleep(0.3)
        s.close()
    except Exception:
        pass
    time.sleep(3.5)


def run_preset(
    tx_port: str,
    rx_port: str,
    baudrate: int,
    preset_name: str,
    count: int,
    power: int,
    rx_window: float,
) -> tuple[int, int]:
    """Run a single preset OTA round-trip. Returns (matched, total_rx)."""
    from feralrf import PHY, PROP_PRESETS, Radio, RxStreamError

    marker = b"\xDE\xAD\xBE\xEF"
    preset = PROP_PRESETS[preset_name]

    reset_cc1352(tx_port)
    reset_cc1352(rx_port)

    tx = Radio(port=tx_port, baudrate=baudrate)
    rx = Radio(port=rx_port, baudrate=baudrate)

    try:
        tx.init()
        rx.init()

        tx.set_phy(PHY.PROPRIETARY_GFSK, channel=0)
        rx.set_phy(PHY.PROPRIETARY_GFSK, channel=0)
        tx.configure_prop(**preset)
        rx.configure_prop(**preset)
        tx.set_power(power)

        rx.start_rx()
        time.sleep(0.3)

        for _ in range(count):
            tx.transmit(marker, power_dbm=power)
            time.sleep(0.1)

        time.sleep(1.0)

        matched = 0
        total = 0
        for pkt in rx.read_packets(timeout=rx_window):
            if isinstance(pkt, RxStreamError):
                continue
            total += 1
            if marker in pkt.data:
                matched += 1

        try:
            rx.stop_rx()
        except Exception:
            pass

        return matched, total
    finally:
        tx.disconnect()
        rx.disconnect()


def main() -> int:
    parser = argparse.ArgumentParser(description="F29 Sub-G 915 MHz preset smoke")
    parser.add_argument("--tx-port", required=True, help="TX board (no Sub-1GHz fault)")
    parser.add_argument("--rx-port", required=True, help="RX board")
    parser.add_argument("--baudrate", type=int, default=921600)
    parser.add_argument("--count", type=int, default=10, help="Markers per preset")
    parser.add_argument("--min-markers", type=int, default=10, help="Min markers/preset for PASS")
    parser.add_argument("--power", type=int, default=0)
    parser.add_argument("--rx-window", type=float, default=2.0)
    args = parser.parse_args()

    print("F29 vuelta 1 — Sub-G 915 MHz preset smoke")
    print(f"TX={args.tx_port} RX={args.rx_port} count={args.count} power={args.power} dBm")
    print("=" * 60)

    results = []
    for name in F29_PRESETS:
        print(f"\n[ -- ] {name}")
        try:
            matched, total = run_preset(
                args.tx_port,
                args.rx_port,
                args.baudrate,
                name,
                args.count,
                args.power,
                args.rx_window,
            )
        except Exception as exc:
            print(f"[FAIL] {name}: exception {exc!r}")
            results.append((name, 0, 0, False))
            continue

        passed = matched >= args.min_markers
        status = "PASS" if passed else "FAIL"
        print(f"[{status}] {name}: markers={matched}/{args.count} total_rx={total}")
        results.append((name, matched, total, passed))

    print("\n" + "=" * 60)
    total_matched = sum(r[1] for r in results)
    expected = args.count * len(F29_PRESETS)
    all_pass = all(r[3] for r in results)
    print(f"Aggregate: {total_matched}/{expected} markers, " f"{sum(r[3] for r in results)}/{len(results)} presets pass")

    return 0 if all_pass else 1


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 2: Verificar que el script parsea y `--help` funciona**

```bash
cd python && PYTHONPATH=. python examples/smoke_f29_subg_915.py --help 2>&1 | head -15
```

Expected: argparse help output, exit 0. Si falla, hay un syntax error — revisar.

- [ ] **Step 3: Pre-commit + commit**

```bash
cd /home/sabas/Documents/electroniccats/FeralRF
pre-commit run --files python/examples/smoke_f29_subg_915.py
git add python/examples/smoke_f29_subg_915.py
git commit -m "$(cat <<'EOF'
test(f29): smoke marker test multi-preset Sub-G 915 MHz

Add python/examples/smoke_f29_subg_915.py — recorre los 3 presets F29
(sidewalk_915_fsk_50k, sidewalk_915_fsk_250k, wisun_915_fsk_50k) y para
cada uno valida marker round-trip TX→RX entre 2 CatSniffer boards.

Pass criteria: >= 10 markers por preset (30/30 agregado). Patrón derivado
de smoke_ota_txrx.py extendido a multi-preset in-process.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

Expected: pre-commit clean (puede haber reformat black/isort), commit lands.

---

## Task 3: Pre-flight bisección — validar baseline `gfsk_868_50k`

**Files:**
- None (hardware test)

**Background:** Antes de ejecutar el F29 smoke (banda nueva 915 MHz), validar que la cadena 2-board OTA funciona con un preset YA validado (`gfsk_868_50k`). Esto separa "problema F29 / banda 915" de "problema HW / setup". Los puertos de los 2 boards los toma del estado del usuario al momento de correr (variables).

- [ ] **Step 1: Identificar puertos de los 2 boards**

```bash
ls /dev/ttyACM*
```

Esperado: 4 ttyACM* visibles si tenés 2 boards conectados. Cada board expone 2 puertos (data + shell). Por convención del repo:
- Board #1: `/dev/ttyACM2` (data) + `/dev/ttyACM4` (shell)
- Board #2: `/dev/ttyACM0` o `/dev/ttyACM5` (data, según orden de plug)

Confirmar manualmente cuál es TX (no fault Sub-1GHz) y cuál es RX (board #1).

- [ ] **Step 2: Correr smoke 868 MHz baseline**

```bash
cd python && PYTHONPATH=. python examples/smoke_ota_txrx.py \
    --tx-port <TX_PORT> --rx-port <RX_PORT> \
    --preset gfsk_868_50k --count 10 --min-markers 10
```

Expected: `[OTA ] preset=gfsk_868_50k: markers=10/10 total_rx=>=10` y exit 0. Si markers < 10, **STOP** y diagnosticar HW/cableado/distancia ANTES de continuar a F29 (problema no relacionado con F29).

- [ ] **Step 3: Si pre-flight OK, registrar mentalmente los puertos para usar en Task 4**

No requiere edit ni commit. Solo log mental: `TX=/dev/ttyACMx`, `RX=/dev/ttyACMy`.

---

## Task 4: Smoke F29 10/10 × 3

**Files:**
- None (hardware test)

- [ ] **Step 1: Ejecutar smoke F29 multi-preset**

```bash
cd python && PYTHONPATH=. python examples/smoke_f29_subg_915.py \
    --tx-port <TX_PORT> --rx-port <RX_PORT> \
    --count 10 --min-markers 10
```

Expected (PASS): cada preset reporta `[PASS] <name>: markers=10/10`, agregado `30/30`, exit 0.

Expected (FAIL parcial): si solo `sidewalk_915_fsk_250k` falla, plan B: ajustar `rx_bw` a `0x5A` (más ancho) o `0x4E` (más angosto) en `presets.py` — re-edit, re-test unitario, re-flash NO necesario (firmware no cambia), re-correr smoke.

Expected (FAIL todos): plan B: probar con `mod_type=1` (GFSK) en lugar de `0` (FSK puro). Editar los 3 presets, re-correr Task 1 step 4-5 (tests unitarios) ajustando `test_f29_preset_uses_fsk` o renombrando. Re-correr smoke. Si sigue fallando, escalar a investigación — la banda 915 puede requerir overrides específicos del SDK que no están en el path actual.

- [ ] **Step 2: Si smoke PASS, opcional re-correr para validar repetibilidad**

```bash
cd python && PYTHONPATH=. python examples/smoke_f29_subg_915.py \
    --tx-port <TX_PORT> --rx-port <RX_PORT> \
    --count 10 --min-markers 10
```

Expected: idem, 30/30. Confirma no es flake.

- [ ] **Step 3: Si plan B fue necesario, commit el ajuste antes del tag**

Si se cambió mod_type / rx_bw / deviation en alguno de los presets:

```bash
pre-commit run --files python/feralrf/presets.py python/tests/test_props.py
git add python/feralrf/presets.py python/tests/test_props.py
git commit -m "$(cat <<'EOF'
fix(f29): preset adjustments after wire-level smoke

<Describir qué cambió y por qué — ej. wisun_915_fsk_50k symbol_rate
ajustado a 50000 con mod_type=1 GFSK porque mod_type=0 FSK silencio
total en marker test>

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

(Saltar este step si Task 4 step 1 pasó al primer intento.)

---

## Task 5: Tag, memory, FF merge

**Files:**
- Memory: `~/.claude/projects/-home-sabas-Documents-electroniccats-FeralRF/memory/project_f29_partial.md` (new)
- Memory: `~/.claude/projects/-home-sabas-Documents-electroniccats-FeralRF/memory/MEMORY.md` (extend index)
- Memory: `~/.claude/projects/-home-sabas-Documents-electroniccats-FeralRF/memory/project_next_session.md` (mark F29.b as next)

- [ ] **Step 1: Verificar commits del bundle**

```bash
git log --oneline main..feature/f29-stack-presets
```

Expected: 2-3 commits — `feat(f29): add 3 Sub-G 915 MHz presets`, `test(f29): smoke marker test`, opcionalmente `fix(f29): preset adjustments`.

- [ ] **Step 2: Tag `v2.0-f29-partial`**

```bash
git tag -a v2.0-f29-partial -m "F29 vuelta 1 — Sub-G 915 MHz Sidewalk + Wi-SUN baseline (3 presets, smoke 30/30 OTA)"
git tag -l | grep f29
```

Expected: `v2.0-f29-partial` aparece. (Si existe `v2.0-f26-partial` u otros tags partial, conviven.)

- [ ] **Step 3: Crear memory entry `project_f29_partial.md`**

```bash
cat > ~/.claude/projects/-home-sabas-Documents-electroniccats-FeralRF/memory/project_f29_partial.md <<'EOF'
---
name: project_f29_partial
description: F29 vuelta 1 cerrada — 3 presets Sub-G 915 MHz (Sidewalk + Wi-SUN baseline) validados OTA 30/30
type: project
---

F29 PARTIAL closed 2026-05-03. Branch `feature/f29-stack-presets`, tag
`v2.0-f29-partial`. FF'd into `main`.

**3 presets nuevos en `PROP_PRESETS`** (Python-only, cero cambios firmware):
- `sidewalk_915_fsk_50k` — 915 MHz, FSK 50k, deviation 100, rx_bw 0x52
- `sidewalk_915_fsk_250k` — 915 MHz, FSK 250k, deviation 200, rx_bw 0x56
- `wisun_915_fsk_50k` — 902.2 MHz (FAN 1.0 NA-1 ch 0), FSK 50k, deviation 100, rx_bw 0x52

**Fidelidad MEDIA:** banda y symbol rate del estándar real; deviation/rx_bw
heredados del template GFSK 868 validado; sync word genérico 0x930B51DE.

**Smoke OTA:** 30/30 markers (10/10 × 3 presets) entre 2 boards CatSniffer.
TX desde board #2, RX desde board #1. Antena CatSniffer cubre 868 y 915
sin issue.

**Tests:** 4 funciones F29-specific (parametrized × 3 names) en
`python/tests/test_props.py` + 3 hits parametrize del schema test existente.

**Why:** Master plan v2.0 §F29. Vuelta 1 enfocada en banda 915 MHz US ISM
para los protocolos más comunes (Sidewalk + Wi-SUN). Establece baseline
para F29.b.

**How to apply:** Para sniffing/TX en banda 915 MHz, usar
`radio.configure_prop(**PROP_PRESETS["sidewalk_915_fsk_50k"])` o equivalente.
NO sustituye stack del protocolo (Sidewalk requiere autenticación, Wi-SUN
requiere FAN PHY + MAC, etc) — solo provee identidad PHY mínima.

**Out of scope (deferred to F29.b):**
- Wi-SUN FAN data rates 100/150/200/300 kbps
- MIOTY TS-UNB 396 baud (investigar si CC1352 lo soporta nativamente)
- Sidewalk LR (LoRa, NO soportado por CC1352, requiere SX1262)
- Sync words spec-correct
- Channel hopping plans (Wi-SUN FAN sequence)
- Validación contra hardware real Sidewalk gateway / Wi-SUN node (v2.1)
EOF
```

- [ ] **Step 4: Actualizar `MEMORY.md` index**

Edit `~/.claude/projects/-home-sabas-Documents-electroniccats-FeralRF/memory/MEMORY.md`. Después de la línea `project_f8f_done` agregar:

```
- [project_f29_partial.md](project_f29_partial.md) — 2026-05-03 tag v2.0-f29-partial. 3 presets Sub-G 915 MHz Sidewalk + Wi-SUN baseline. Smoke 30/30 OTA. F29.b cubre MIOTY + Wi-SUN FAN completo + spec-correct sync.
```

- [ ] **Step 5: Actualizar `project_next_session.md`**

Edit el archivo, en la sección "Candidate priorities for next session", reemplazar la entrada actual de F29 (si existe) por F29.b como next iteration. Si no hay entrada F29 explícita, agregar:

```
### N. F29.b — MIOTY + Wi-SUN FAN completo + Sidewalk fidelidad alta
Continuación de F29 vuelta 1 (tag v2.0-f29-partial). Scope:
- Wi-SUN FAN 1.0 data rates 100/150/200/300 kbps (4 presets adicionales)
- MIOTY TS-UNB 396 baud — investigar si CC1352 soporta nativamente o
  requiere truco (modulation index, custom override)
- Sync words spec-correct para los 3 presets de vuelta 1
- Channel hopping plans (Wi-SUN FAN NA-1 sequence)
Estimado: 1-2 sesiones, mayor riesgo en MIOTY (no validado nativamente).
```

(Renumerar otras prioridades si es necesario.)

- [ ] **Step 6: FF merge a `main`**

```bash
cd /home/sabas/Documents/electroniccats/FeralRF
git checkout main
git merge --ff-only feature/f29-stack-presets
git log --oneline -5
```

Expected: FF succeeds (sin merge commit). HEAD = último commit del bundle.

- [ ] **Step 7: Sync `feature/ti-rtos-migration` a `main`** (mantiene paridad pos-FF a main)

```bash
git update-ref refs/heads/feature/ti-rtos-migration main
git rev-parse main feature/ti-rtos-migration
```

Expected: ambas refs apuntan al mismo SHA.

- [ ] **Step 8: Push** (interactivo desde shell del usuario, no autonomous)

User runs:
```
! git push origin main feature/ti-rtos-migration v2.0-f29-partial
```

NO ejecutar push autónomo per safety rules — esperar confirmación del usuario antes de publicar.

---

## Self-review checklist (run before declaring plan complete)

- [ ] **Spec coverage:** spec lista entregables Python (3 presets, tests, smoke), criterios (30/30 markers, tests pass), out-of-scope (MIOTY, Wi-SUN FAN completo, sync correct). Plan tiene Task 1 (presets+tests), Task 2 (smoke script), Task 3-4 (smoke ejecución), Task 5 (tag+memory+merge). ✅
- [ ] **Placeholder scan:** ningún "TBD", "TODO", "fill in", "similar to". Step de plan B en Task 4 referencia valores específicos (`mod_type=1`, `rx_bw=0x5A`/`0x4E`). ✅
- [ ] **Type/symbol consistency:** `PROP_PRESETS`, `Radio`, `RxStreamError`, `PHY.PROPRIETARY_GFSK`, `radio.configure_prop`, `radio.set_phy`, `radio.transmit`, `radio.read_packets` todos verificados contra HEAD `f50c5e3`. Preset names consistentes en presets.py + test_props.py + smoke. ✅
- [ ] **Hardware:** TX board #2 / RX board #1 documentado. Pre-flight bisección con `gfsk_868_50k` para descartar HW antes de F29.  ✅
- [ ] **Compat:** spec dice "cero cambios firmware". Plan no toca nada en `firmware/cc1352/`. ✅

Si algo falla, fix inline antes del handoff.
