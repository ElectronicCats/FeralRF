# F29.b Cierre Completo Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Cerrar §F29 completo: 1 preset MIOTY TS-UNB + 4 presets Wi-SUN FAN restantes + 3 demos stub + comment doc Sidewalk LR. Smoke 80/80 markers OTA si MIOTY responde; tag `v2.0-f29` o `v2.0-f29-near-final` con escape M3.

**Architecture:** Branch `feature/f29b-cierre` desde `main` HEAD `9d12fd6`. 5 commits secuenciales (MIOTY, Wi-SUN, demos, doc, tag). Bundle 1 (MIOTY) primero por riesgo — escape M3 documentado si CC1352 no soporta nativamente. Cero cambios firmware.

**Tech Stack:** Python 3.11+ (pyserial, pytest), pre-commit. CC1352 firmware ya validado en sesión F29 vuelta 1 — ambos boards CatSniffer ya flasheados con `feralrf_cc1352.elf` HEAD `04caba9`.

**Spec source:** `docs/superpowers/specs/2026-05-03-f29b-cierre-design.md` (commit `9d12fd6`).

**Hardware:** TX board #2 (`/dev/ttyACM1`, sin Sub-1GHz fault), RX board #1 (`/dev/ttyACM2`, Sub-1GHz RX OK). Validados en F29 vuelta 1 con 30/30 markers.

---

## Task 0: Crear working branch

**Files:**
- None (git operation)

- [ ] **Step 1: Verificar baseline en HEAD `9d12fd6`**

```bash
git status
git log --oneline -3
```

Expected: HEAD = `9d12fd6 plan(f29b): cierre completo §F29 spec`. Working tree limpio.

- [ ] **Step 2: Crear branch `feature/f29b-cierre`**

```bash
git checkout -b feature/f29b-cierre
git status
```

Expected: branch creada, working tree limpio.

---

## Task 1: MIOTY TS-UNB preset + smoke aislado (con escape M3)

**Files:**
- Modify: `python/feralrf/presets.py` (agregar 1 entrada MIOTY)
- Modify: `python/tests/test_props.py` (agregar tests F29.b parciales para MIOTY)

**Background:** MIOTY TS-UNB usa 396 baud (ETSI TS 103 357), banda 868 MHz EU SRD. Es el ítem de mayor riesgo del cierre F29 — el `rx_bw` mínimo del CC1352 puede no llegar al ~5 kHz necesario para demodular TS-UNB. Test aislado primero; escape M3 documentado paso a paso.

- [ ] **Step 1: Tests F29.b para MIOTY (failing)**

Append al final de `python/tests/test_props.py` (después del bloque F29 vuelta 1):

```python
# F29.b — cierre completo (MIOTY + Wi-SUN restantes)


F29B_MIOTY_NAME = "mioty_868_tsunb"
F29B_WISUN_NAMES = (
    "wisun_915_fsk_100k",
    "wisun_915_fsk_150k",
    "wisun_915_fsk_200k",
    "wisun_915_fsk_300k",
)
F29B_PRESET_NAMES = (F29B_MIOTY_NAME,) + F29B_WISUN_NAMES


def test_f29b_mioty_present():
    """F29.b — MIOTY TS-UNB preset presente."""
    assert F29B_MIOTY_NAME in PROP_PRESETS


def test_f29b_mioty_in_868_band():
    """F29.b — MIOTY TS-UNB en banda 868 EU SRD (863-870 MHz)."""
    f = PROP_PRESETS[F29B_MIOTY_NAME]["frequency_hz"]
    assert 863_000_000 <= f <= 870_000_000


def test_f29b_mioty_symbol_rate_396():
    """F29.b — MIOTY symbol rate per ETSI TS 103 357 (396 baud)."""
    assert PROP_PRESETS[F29B_MIOTY_NAME]["symbol_rate"] == 396
```

- [ ] **Step 2: Verificar que los tests fallan**

```bash
cd python && PYTHONPATH=. pytest tests/test_props.py::test_f29b_mioty_present tests/test_props.py::test_f29b_mioty_in_868_band tests/test_props.py::test_f29b_mioty_symbol_rate_396 -v 2>&1 | tail -10
```

Expected: 3 FAIL — KeyError o assert por preset ausente.

- [ ] **Step 3: Agregar preset MIOTY a `presets.py`**

Buscar el bloque "902-928 MHz ISM Band" en `python/feralrf/presets.py` y agregar el bloque MIOTY justo después del último Wi-SUN preset, antes del cierre del dict.

```bash
grep -n "wisun_915_fsk_50k\|^}" python/feralrf/presets.py
```

Insertar las nuevas entradas después de `wisun_915_fsk_50k` (último de F29 vuelta 1):

```python
    # === MIOTY TS-UNB (ETSI TS 103 357) — 868 MHz EU SRD ===
    # WARNING: 396 baud es ultra-low-rate. Viability sobre CC1352 pendiente
    # validación OTA en Task 1 del plan F29.b. Si rx_bw mínimo del CC1352
    # no llega a ~5 kHz, escape M3: probar GFSK, escalación rx_bw, fallback
    # a "pending native support" excluido del smoke loop.
    "mioty_868_tsunb": dict(
        frequency_hz=868000000,
        mod_type=0,  # FSK puro como primer intento
        symbol_rate=396,  # ETSI TS 103 357
        deviation=1,  # ~250 Hz unidad CC1352 → mod index ~1.3
        rx_bw=0x4A,  # mínimo BW soportado típicamente ~5 kHz
        sync_word=0x930B51DE,
    ),
```

- [ ] **Step 4: Tests pasan**

```bash
cd python && PYTHONPATH=. pytest tests/test_props.py::test_f29b_mioty_present tests/test_props.py::test_f29b_mioty_in_868_band tests/test_props.py::test_f29b_mioty_symbol_rate_396 -v 2>&1 | tail -5
```

Expected: 3 PASS.

- [ ] **Step 5: Suite completa Python (regression)**

```bash
PYTHONPATH=. pytest -q 2>&1 | tail -5
```

Expected: ≥ 491 pass (era 488 + 3 nuevos), 0 fail, 5 skipped.

- [ ] **Step 6: Pre-commit + commit MIOTY preset**

```bash
cd /home/sabas/Documents/electroniccats/FeralRF
pre-commit run --files python/feralrf/presets.py python/tests/test_props.py
git add python/feralrf/presets.py python/tests/test_props.py
git commit -m "$(cat <<'EOF'
feat(f29b): add MIOTY TS-UNB preset (868 MHz, 396 baud)

Add mioty_868_tsunb preset per ETSI TS 103 357: 868 MHz EU SRD,
symbol_rate=396 baud, FSK puro como primer intento. Viability OTA
pendiente Task 1 smoke aislado del plan F29.b — si CC1352 no soporta
nativamente el ultra-low rate / narrow BW, escape M3 documentado.

Add 3 F29.b tests (presente, banda 868, sym rate 396).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

Expected: pre-commit clean (puede haber reformat black de la entry), commit lands.

- [ ] **Step 7: HUMAN CHECKPOINT — confirmar 2 boards disponibles**

Antes del smoke MIOTY aislado, confirmar con el usuario:
- Board #1 conectado en `/dev/ttyACM2` (RX, ya flasheado F8f follow-up)
- Board #2 conectado en `/dev/ttyACM1` (TX, ya flasheado F8f follow-up)
- Boards a corta distancia (mismo escritorio)

```bash
ls /dev/ttyACM*
```

Si los boards no están, **STOP y esperar**.

- [ ] **Step 8: Smoke MIOTY aislado — primer intento (FSK puro)**

Crear un mini-smoke ad-hoc o reutilizar `smoke_ota_txrx.py` con `--preset mioty_868_tsunb`:

```bash
cd python && PYTHONPATH=. python examples/smoke_ota_txrx.py \
    --tx-port /dev/ttyACM1 --rx-port /dev/ttyACM2 \
    --preset mioty_868_tsunb --count 10 --min-markers 10 2>&1 | tail -10
```

Expected (PASS): `[OTA ] preset=mioty_868_tsunb: markers=10/10 total_rx=>=10` y exit 0.

Expected (FAIL): markers=0/10 → activar **escape M3** (Step 9).

- [ ] **Step 9: Escape M3 (solo si Step 8 fail) — switch a GFSK**

**Solo ejecutar si Step 8 reportó 0/10 markers.** Modificar el preset MIOTY en `presets.py`:

```python
    "mioty_868_tsunb": dict(
        frequency_hz=868000000,
        mod_type=1,  # M3.1: GFSK fallback (FSK puro 0/10 en Step 8)
        symbol_rate=396,
        deviation=1,
        rx_bw=0x4A,
        sync_word=0x930B51DE,
    ),
```

Re-correr smoke:

```bash
cd python && PYTHONPATH=. python examples/smoke_ota_txrx.py \
    --tx-port /dev/ttyACM1 --rx-port /dev/ttyACM2 \
    --preset mioty_868_tsunb --count 10 --min-markers 10 2>&1 | tail -10
```

Si PASS, commit el ajuste:

```bash
cd /home/sabas/Documents/electroniccats/FeralRF
pre-commit run --files python/feralrf/presets.py
git add python/feralrf/presets.py
git commit -m "fix(f29b): MIOTY mod_type=1 (GFSK) — FSK puro 0/10 en wire-level smoke"
```

Si sigue 0/10, ir a Step 10.

- [ ] **Step 10: Escape M3.2 (solo si Step 9 fail) — escalación rx_bw**

**Solo ejecutar si Step 9 todavía reporta 0/10.** Probar 3 valores rx_bw, uno a la vez. Para cada uno: editar preset, correr smoke, si fail, siguiente.

```python
    # Intento M3.2.a: rx_bw=0x46 (más angosto)
    "mioty_868_tsunb": dict(..., rx_bw=0x46, ...),
```

```bash
cd python && PYTHONPATH=. python examples/smoke_ota_txrx.py \
    --tx-port /dev/ttyACM1 --rx-port /dev/ttyACM2 \
    --preset mioty_868_tsunb --count 10 --min-markers 10 2>&1 | tail -5
```

Si PASS → commit con mensaje específico (`rx_bw=0x46`).

Si FAIL → probar `rx_bw=0x42` (aún más angosto):

```python
    "mioty_868_tsunb": dict(..., rx_bw=0x42, ...),
```

Re-correr smoke. Si PASS → commit. Si FAIL → probar `rx_bw=0x4E` (un poco más ancho, contra-intuitivo pero por descarte):

```python
    "mioty_868_tsunb": dict(..., rx_bw=0x4E, ...),
```

Si todos fallan → ir a Step 11.

- [ ] **Step 11: Escape M3.3 (solo si Steps 9-10 fail) — exclude MIOTY del smoke**

**Solo ejecutar si todas las variantes M3.1-M3.2 fallaron.** El preset queda en `PROP_PRESETS` para que tests unitarios pasen, pero se marca como pending y se excluye del smoke loop.

Modificar el comment del preset en `presets.py`:

```python
    # === MIOTY TS-UNB (ETSI TS 103 357) — 868 MHz EU SRD ===
    # PENDING NATIVE SUPPORT: validación OTA F29.b en 2026-05-03 falló
    # (escape M3 completo: FSK puro 0/10, GFSK 0/10, rx_bw 0x4A/0x46/0x42/0x4E
    # todos 0/10). CC1352 puede no soportar TS-UNB 396 baud nativamente sin
    # patch CPE custom. Preset retenido para preservar API ID; smoke F29.b
    # excluye del loop hasta que F29.c (con SmartRF Studio + custom override)
    # cierre el caso. Ver project_f29_done.md y master plan §F29.
    "mioty_868_tsunb": dict(
        frequency_hz=868000000,
        mod_type=0,
        symbol_rate=396,
        deviation=1,
        rx_bw=0x4A,
        sync_word=0x930B51DE,
    ),
```

Commit:

```bash
cd /home/sabas/Documents/electroniccats/FeralRF
pre-commit run --files python/feralrf/presets.py
git add python/feralrf/presets.py
git commit -m "$(cat <<'EOF'
fix(f29b): mark MIOTY TS-UNB as pending native support — escape M3 complete

Escape M3 exhausted (FSK 0/10, GFSK 0/10, rx_bw 0x4A/0x46/0x42/0x4E all
0/10). CC1352 likely needs custom CPE patch / override to demodulate
TS-UNB 396 baud at this BW. Defer to F29.c with SmartRF Studio access.

Preset stays in PROP_PRESETS to preserve API ID; smoke F29.b loop
excludes mioty_868_tsunb. Tag will be v2.0-f29-near-final not v2.0-f29.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

Set una variable mental: **MIOTY_EXCLUDED=true** — afecta tasks 2, 5 (smoke loop y tag final).

---

## Task 2: Wi-SUN FAN 1.0 — 4 presets restantes

**Files:**
- Modify: `python/feralrf/presets.py` (agregar 4 entradas Wi-SUN)
- Modify: `python/tests/test_props.py` (agregar tests F29.b para Wi-SUN)

**Background:** Master plan §F29 dice "FSK 2-FSK 50/100/150/200/300 kbps". Vuelta 1 hizo 50k. Faltan 4 rates. Para FSK puro a 200k+ usar `rx_bw=0x5A` (lección F29 vuelta 1). Para 300k posiblemente `0x5E`.

- [ ] **Step 1: Tests F29.b para Wi-SUN (failing)**

Append al final de `python/tests/test_props.py`:

```python
@pytest.mark.parametrize("name", F29B_WISUN_NAMES)
def test_f29b_wisun_present(name):
    """F29.b — los 4 Wi-SUN nuevos están en PROP_PRESETS."""
    assert name in PROP_PRESETS, f"Preset {name} ausente"


@pytest.mark.parametrize("name", F29B_WISUN_NAMES)
def test_f29b_wisun_in_915_band(name):
    """F29.b — Wi-SUN nuevos en banda 902-928 MHz."""
    f = PROP_PRESETS[name]["frequency_hz"]
    assert 902_000_000 <= f <= 928_000_000


@pytest.mark.parametrize("name", F29B_WISUN_NAMES)
def test_f29b_wisun_uses_fsk(name):
    """F29.b — Wi-SUN usa FSK puro (mod_type=0)."""
    assert PROP_PRESETS[name]["mod_type"] == 0


def test_f29b_wisun_count():
    """F29.b — exactamente 4 Wi-SUN nuevos."""
    wisun_new = [n for n in PROP_PRESETS if n in F29B_WISUN_NAMES]
    assert len(wisun_new) == 4


def test_f29b_preset_count():
    """F29.b — exactamente 5 presets nuevos (1 MIOTY + 4 Wi-SUN)."""
    f29b = [n for n in PROP_PRESETS if n in F29B_PRESET_NAMES]
    assert len(f29b) == 5
```

- [ ] **Step 2: Verificar que los tests Wi-SUN fallan**

```bash
cd python && PYTHONPATH=. pytest tests/test_props.py::test_f29b_wisun_present tests/test_props.py::test_f29b_wisun_count -v 2>&1 | tail -10
```

Expected: 5 FAIL (4 parametrize + 1 count) — KeyError / assert por presets ausentes.

- [ ] **Step 3: Agregar 4 presets Wi-SUN a `presets.py`**

Insertar JUSTO después del bloque MIOTY (Task 1 step 3) y antes del cierre del dict:

```python
    # === Wi-SUN FAN 1.0 NA-1 ch 0 — data rates restantes (F29.b) ===
    # Mod index implícito ~0.5; deviation pragmática heredada de templates.
    # F29 vuelta 1 lección: FSK puro >=150k requiere rx_bw=0x5A; 300k probablemente 0x5E.
    "wisun_915_fsk_100k": dict(
        frequency_hz=902200000,
        mod_type=0,
        symbol_rate=100000,
        deviation=150,
        rx_bw=0x56,  # match gfsk_868_100k validado
        sync_word=0x930B51DE,
    ),
    "wisun_915_fsk_150k": dict(
        frequency_hz=902200000,
        mod_type=0,
        symbol_rate=150000,
        deviation=200,
        rx_bw=0x5A,  # FSK puro >= 150k → más BW
        sync_word=0x930B51DE,
    ),
    "wisun_915_fsk_200k": dict(
        frequency_hz=902200000,
        mod_type=0,
        symbol_rate=200000,
        deviation=200,
        rx_bw=0x5A,  # F29 vuelta 1 lección
        sync_word=0x930B51DE,
    ),
    "wisun_915_fsk_300k": dict(
        frequency_hz=902200000,
        mod_type=0,
        symbol_rate=300000,
        deviation=200,
        rx_bw=0x5E,  # rate más alto requiere BW más amplio
        sync_word=0x930B51DE,
    ),
```

- [ ] **Step 4: Tests Wi-SUN pasan + suite completa**

```bash
cd python && PYTHONPATH=. pytest tests/test_props.py -v 2>&1 | tail -15
```

Expected: TODOS los tests F29.b Wi-SUN pasan. Total ≈ 24 nuevos parametrize hits + 5 schema parametrize.

```bash
PYTHONPATH=. pytest -q 2>&1 | tail -5
```

Expected: ≥ 512 pass total (488 base + 24 nuevos), 0 fail, 5 skipped.

- [ ] **Step 5: Pre-commit + commit Wi-SUN**

```bash
cd /home/sabas/Documents/electroniccats/FeralRF
pre-commit run --files python/feralrf/presets.py python/tests/test_props.py
git add python/feralrf/presets.py python/tests/test_props.py
git commit -m "$(cat <<'EOF'
feat(f29b): add 4 Wi-SUN FAN 1.0 presets — 100/150/200/300 kbps

Add wisun_915_fsk_100k, wisun_915_fsk_150k, wisun_915_fsk_200k,
wisun_915_fsk_300k completando los 5 data rates listados en master
plan §F29 ("FSK 2-FSK 50/100/150/200/300 kbps"). Mod index implícito
~0.5. Patrón rx_bw escalation:
  - 100k: 0x56 (match gfsk_868_100k validado)
  - 150k: 0x5A (FSK puro >= 150k necesita más BW)
  - 200k: 0x5A (F29 vuelta 1 lección, sidewalk_915_fsk_250k)
  - 300k: 0x5E (rate más alto, BW más amplio; ajustar tras smoke)

Add 4 parametrize tests + count assertion. Total tests F29.b: 7 nuevos
(MIOTY) + 13 nuevos (Wi-SUN) ≈ 20 unique hits.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

- [ ] **Step 6: Extender smoke `smoke_f29_subg_915.py` con los 5 nuevos**

Editar `python/examples/smoke_f29_subg_915.py`. Buscar la tuple `F29_PRESETS`:

```bash
grep -n "F29_PRESETS = " python/examples/smoke_f29_subg_915.py
```

Reemplazar la tuple por:

```python
F29_PRESETS = (
    # F29 vuelta 1
    "sidewalk_915_fsk_50k",
    "sidewalk_915_fsk_250k",
    "wisun_915_fsk_50k",
    # F29.b — Task 2
    "wisun_915_fsk_100k",
    "wisun_915_fsk_150k",
    "wisun_915_fsk_200k",
    "wisun_915_fsk_300k",
    # F29.b — Task 1 MIOTY (incluido SOLO si M3 no se activó)
    "mioty_868_tsunb",
)
```

**Si el escape M3.3 (Step 11 de Task 1) se activó** y MIOTY quedó como pending, **REMOVER** la línea `"mioty_868_tsunb",` de la tuple. El smoke quedará en 7 presets / 70 markers en lugar de 8 / 80.

También actualizar el docstring del archivo:

```python
"""F29 cierre completo — Sub-G 915 + 868 MHz preset smoke (8 presets total).

Cubre F29 vuelta 1 (3 presets) + F29.b Task 1-2 (5 presets nuevos: MIOTY +
4 Wi-SUN). Pass total = 80/80 markers (10 × 8); 70/70 si MIOTY excluido
por escape M3.

Usage:
    python smoke_f29_subg_915.py --tx-port /dev/ttyACM1 --rx-port /dev/ttyACM2
"""
```

- [ ] **Step 7: HUMAN CHECKPOINT — antes del smoke completo**

Confirmar con el usuario que ambos boards siguen disponibles. Smoke estimado: ~4-5 minutos (8 presets × ~30s c/u).

- [ ] **Step 8: Smoke F29 completo**

```bash
cd python && PYTHONPATH=. python examples/smoke_f29_subg_915.py \
    --tx-port /dev/ttyACM1 --rx-port /dev/ttyACM2 \
    --count 10 --min-markers 10 2>&1 | tail -25
```

Expected (path principal): `Aggregate: 80/80 markers, 8/8 presets pass` y exit 0.

Expected (path escape M3): `Aggregate: 70/70 markers, 7/7 presets pass` (MIOTY excluido) y exit 0.

Expected (FAIL parcial Wi-SUN): si por ej. 300k falla 0/10, plan B en sitio: cambiar `rx_bw=0x5E` → `0x62`, re-test, commit fix con mensaje `fix(f29b): wisun_915_fsk_300k rx_bw=0x62 after wire-level smoke`.

- [ ] **Step 9: Pre-commit + commit smoke extension**

```bash
cd /home/sabas/Documents/electroniccats/FeralRF
pre-commit run --files python/examples/smoke_f29_subg_915.py
git add python/examples/smoke_f29_subg_915.py
git commit -m "$(cat <<'EOF'
test(f29b): extend smoke to cover 8 presets (F29 + F29.b)

Smoke ahora cubre los 3 presets de F29 vuelta 1 + 5 presets nuevos de
F29.b (4 Wi-SUN restantes + MIOTY si Task 1 PASS). Pass criterio:
80/80 markers; 70/70 si MIOTY excluido por escape M3.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

(Si hubo Plan B Wi-SUN ajustando rx_bw, ese commit es separado y va antes de éste.)

---

## Task 3: 3 demo scripts stub

**Files:**
- Create: `python/examples/lab/demo_wisun_scan.py`
- Create: `python/examples/lab/demo_mioty_listen.py`
- Create: `python/examples/lab/demo_sidewalk_subg.py`

**Background:** Master plan §F29 lista los 3 demos: "set_phy con preset + start_rx + parse mínimo". D1 stubs minimalistas — argparse + load preset + RX loop + print hex. NO parsea stack del protocolo.

- [ ] **Step 1: Crear `demo_wisun_scan.py`**

```python
#!/usr/bin/env python3
"""F29 — Wi-SUN FAN 1.0 (915 MHz US ISM) capture stub.

Default preset: wisun_915_fsk_50k. Otros rates via --preset.
Loads preset, starts RX, prints first N bytes hex of each packet.
NO parse de stack (PHY-only). Ctrl-C para salir.
"""
import argparse
import sys

from feralrf import PHY, PROP_PRESETS, Radio, RxStreamError


def main() -> int:
    parser = argparse.ArgumentParser(description="F29 Wi-SUN scan stub")
    parser.add_argument("--port", required=True)
    parser.add_argument(
        "--preset",
        default="wisun_915_fsk_50k",
        choices=[n for n in PROP_PRESETS if n.startswith("wisun_")],
    )
    parser.add_argument("--baudrate", type=int, default=921600)
    parser.add_argument("--max-hex", type=int, default=32, help="Max bytes to hex-print")
    parser.add_argument("--duration", type=float, default=30.0)
    args = parser.parse_args()

    radio = Radio(port=args.port, baudrate=args.baudrate)
    try:
        radio.init()
        radio.set_phy(PHY.PROPRIETARY_GFSK, channel=0)
        radio.configure_prop(**PROP_PRESETS[args.preset])
        radio.start_rx()
        print(f"Scanning preset={args.preset}; Ctrl-C to stop")

        for pkt in radio.read_packets(timeout=args.duration):
            if isinstance(pkt, RxStreamError):
                continue
            head = pkt.data[: args.max_hex].hex()
            crc_flag = "OK" if pkt.crc_ok else "ER"
            print(f"[{crc_flag}] len={len(pkt.data)} rssi={pkt.rssi_dbm} {head}")
    except KeyboardInterrupt:
        pass
    finally:
        radio.disconnect()
    return 0


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 2: Crear `demo_mioty_listen.py`** (mismo template, default mioty)

```python
#!/usr/bin/env python3
"""F29 — MIOTY TS-UNB (868 MHz EU SRD) capture stub.

Default preset: mioty_868_tsunb. Loads preset, starts RX, prints first N
bytes hex of each packet. NO parse de TS-UNB stack (PHY-only). Ctrl-C
para salir. WARNING: si CC1352 no soporta nativamente TS-UNB (escape M3
en F29.b plan), este demo no captura nada — el preset queda como pending.
"""
import argparse
import sys

from feralrf import PHY, PROP_PRESETS, Radio, RxStreamError


def main() -> int:
    parser = argparse.ArgumentParser(description="F29 MIOTY listen stub")
    parser.add_argument("--port", required=True)
    parser.add_argument(
        "--preset",
        default="mioty_868_tsunb",
        choices=[n for n in PROP_PRESETS if n.startswith("mioty_")],
    )
    parser.add_argument("--baudrate", type=int, default=921600)
    parser.add_argument("--max-hex", type=int, default=32)
    parser.add_argument("--duration", type=float, default=30.0)
    args = parser.parse_args()

    radio = Radio(port=args.port, baudrate=args.baudrate)
    try:
        radio.init()
        radio.set_phy(PHY.PROPRIETARY_GFSK, channel=0)
        radio.configure_prop(**PROP_PRESETS[args.preset])
        radio.start_rx()
        print(f"Listening preset={args.preset}; Ctrl-C to stop")

        for pkt in radio.read_packets(timeout=args.duration):
            if isinstance(pkt, RxStreamError):
                continue
            head = pkt.data[: args.max_hex].hex()
            crc_flag = "OK" if pkt.crc_ok else "ER"
            print(f"[{crc_flag}] len={len(pkt.data)} rssi={pkt.rssi_dbm} {head}")
    except KeyboardInterrupt:
        pass
    finally:
        radio.disconnect()
    return 0


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 3: Crear `demo_sidewalk_subg.py`** (mismo template, default sidewalk)

```python
#!/usr/bin/env python3
"""F29 — Amazon Sidewalk Sub-G FSK (915 MHz US ISM) capture stub.

Default preset: sidewalk_915_fsk_50k. Otros (250k) via --preset.
Loads preset, starts RX, prints first N bytes hex of each packet.
NO parse de stack Sidewalk (PHY-only — auth + framing son layer superior
no soportado aquí). Ctrl-C para salir.

Sidewalk LR (LoRa-like) NO soportado en CC1352 — usa SX1262 vía Cat-LoRa.
"""
import argparse
import sys

from feralrf import PHY, PROP_PRESETS, Radio, RxStreamError


def main() -> int:
    parser = argparse.ArgumentParser(description="F29 Sidewalk Sub-G capture stub")
    parser.add_argument("--port", required=True)
    parser.add_argument(
        "--preset",
        default="sidewalk_915_fsk_50k",
        choices=[n for n in PROP_PRESETS if n.startswith("sidewalk_")],
    )
    parser.add_argument("--baudrate", type=int, default=921600)
    parser.add_argument("--max-hex", type=int, default=32)
    parser.add_argument("--duration", type=float, default=30.0)
    args = parser.parse_args()

    radio = Radio(port=args.port, baudrate=args.baudrate)
    try:
        radio.init()
        radio.set_phy(PHY.PROPRIETARY_GFSK, channel=0)
        radio.configure_prop(**PROP_PRESETS[args.preset])
        radio.start_rx()
        print(f"Capturing preset={args.preset}; Ctrl-C to stop")

        for pkt in radio.read_packets(timeout=args.duration):
            if isinstance(pkt, RxStreamError):
                continue
            head = pkt.data[: args.max_hex].hex()
            crc_flag = "OK" if pkt.crc_ok else "ER"
            print(f"[{crc_flag}] len={len(pkt.data)} rssi={pkt.rssi_dbm} {head}")
    except KeyboardInterrupt:
        pass
    finally:
        radio.disconnect()
    return 0


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 4: Verificar que los 3 demos parsean**

```bash
cd python
for f in examples/lab/demo_wisun_scan.py examples/lab/demo_mioty_listen.py examples/lab/demo_sidewalk_subg.py; do
    PYTHONPATH=. python -c "import ast; ast.parse(open('$f').read())" && echo "$f: parse OK" || echo "$f: PARSE FAIL"
done
```

Expected: 3 × `parse OK`.

- [ ] **Step 5: Verificar `--help` funciona en cada uno**

```bash
cd python && for f in examples/lab/demo_wisun_scan.py examples/lab/demo_mioty_listen.py examples/lab/demo_sidewalk_subg.py; do
    PYTHONPATH=. python "$f" --help 2>&1 | head -3
    echo "---"
done
```

Expected: cada uno muestra `usage: <script> [-h] --port PORT ...`.

- [ ] **Step 6: Pre-commit + commit demos**

```bash
cd /home/sabas/Documents/electroniccats/FeralRF
pre-commit run --files python/examples/lab/demo_wisun_scan.py python/examples/lab/demo_mioty_listen.py python/examples/lab/demo_sidewalk_subg.py
git add python/examples/lab/demo_wisun_scan.py python/examples/lab/demo_mioty_listen.py python/examples/lab/demo_sidewalk_subg.py
git commit -m "$(cat <<'EOF'
feat(f29b): add 3 demo capture stubs — Wi-SUN, MIOTY, Sidewalk Sub-G

D1 minimalist stubs per master plan §F29. Cada demo: argparse + load
preset (default acorde al protocolo, --preset selecciona variantes
filtradas por prefix) + start_rx + print first N bytes hex de cada packet.
NO parse de stack del protocolo (PHY-only per feedback_protocol_vs_phy).

Demos:
- demo_wisun_scan.py — default wisun_915_fsk_50k, choices wisun_*
- demo_mioty_listen.py — default mioty_868_tsunb, choices mioty_*
- demo_sidewalk_subg.py — default sidewalk_915_fsk_50k, choices sidewalk_*

ast.parse OK + --help OK validados sin hardware.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 4: Sidewalk LR doc note en presets.py

**Files:**
- Modify: `python/feralrf/presets.py` (agregar comment al final del dict)

**Background:** Master plan §F29 dice "Documentar que Sidewalk LR (LoRa-like) NO es soportado por CC1352 — usa SX1262 en Cat-LoRa port." Comment en `presets.py` cubre el requerimiento sin nuevo archivo.

- [ ] **Step 1: Agregar comment al final del dict**

Editar `python/feralrf/presets.py`. Buscar la línea de cierre del dict:

```bash
grep -n "^}" python/feralrf/presets.py
```

Justo ANTES del `}` final, agregar el comment:

```python
    # === NOTE: Sidewalk LR (LoRa-like) NO soportado por CC1352 ===
    # Sidewalk LR usa LoRa modulation que requiere chipset Semtech (SX1262 o
    # similar). Los presets sidewalk_* arriba cubren SOLO el FSK layer de
    # Sidewalk Sub-G (banda 915 MHz). Para LR, ver hardware Cat-LoRa /
    # Sidewalk LR via SX1262 fuera de este preset dict.
}
```

(El bloque comentario va entre la última entry preset y el `}` que cierra el dict.)

- [ ] **Step 2: Verificar que el dict sigue parseando + tests pasan**

```bash
cd python && PYTHONPATH=. python -c "from feralrf.presets import PROP_PRESETS; print(f'presets: {len(PROP_PRESETS)}')"
```

Expected: `presets: 24` (16 originales + 3 vuelta 1 + 5 F29.b — o 23 si MIOTY pending pero el preset sigue en el dict, sigue siendo 24).

```bash
PYTHONPATH=. pytest tests/test_props.py -q 2>&1 | tail -3
```

Expected: todos pasan.

- [ ] **Step 3: Pre-commit + commit doc note**

```bash
cd /home/sabas/Documents/electroniccats/FeralRF
pre-commit run --files python/feralrf/presets.py
git add python/feralrf/presets.py
git commit -m "$(cat <<'EOF'
docs(f29b): note Sidewalk LR not supported by CC1352

Master plan §F29 entregable: documentar que Sidewalk LR (LoRa-like) usa
modulation Semtech-only (SX1262) y no se soporta en CC1352. Comment
inline en presets.py al final del dict para que cualquiera leyendo el
catalogo encuentre la limitación junto a los presets sidewalk_* (FSK only).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 5: Tag, memory, FF merge

**Files:**
- Memory: `~/.claude/projects/-home-sabas-Documents-electroniccats-FeralRF/memory/project_f29_done.md` (new, supersedes project_f29_partial)
- Memory: `~/.claude/projects/-home-sabas-Documents-electroniccats-FeralRF/memory/MEMORY.md` (extend / replace project_f29_partial reference)
- Memory: `~/.claude/projects/-home-sabas-Documents-electroniccats-FeralRF/memory/project_next_session.md` (mark F29 as fully done)

- [ ] **Step 1: Verificar commits del bundle**

```bash
git log --oneline main..HEAD
```

Expected: 4-7 commits según escape M3 path:
- Mínimo: feat(f29b) MIOTY, feat(f29b) Wi-SUN, test(f29b) smoke extend, feat(f29b) demos, docs(f29b) Sidewalk LR
- Con M3 escape: + commits de mod_type/rx_bw/pending fixes

- [ ] **Step 2: Determinar tag final**

**Si MIOTY pasó OTA (sin escape M3 activado):**
- tag `v2.0-f29` (full closure)

**Si MIOTY se excluyó por escape M3:**
- tag `v2.0-f29-near-final` (4/5 presets nuevos OK, MIOTY pending)

```bash
# Path principal:
git tag -a v2.0-f29 -m "F29 cierre completo — MIOTY + Wi-SUN FAN restantes + demos + Sidewalk LR doc (8 presets total, smoke 80/80)"

# Path escape M3 (descomentar el correcto):
# git tag -a v2.0-f29-near-final -m "F29 cierre near-final — Wi-SUN FAN + demos + Sidewalk LR. MIOTY pending native CC1352 support (escape M3 activated)"

git tag -l | grep f29
```

Expected: ambos tags coexisten — `v2.0-f29-partial` (vuelta 1, histórico) + `v2.0-f29` (o `-near-final`).

- [ ] **Step 3: Crear memory entry `project_f29_done.md`**

Path principal (sustituir `<DETAILS>` con valores reales):

```bash
cat > ~/.claude/projects/-home-sabas-Documents-electroniccats-FeralRF/memory/project_f29_done.md <<'EOF'
---
name: project_f29_done
description: F29 cierre completo — 8 presets Sub-G (3 vuelta 1 + 5 F29.b), demos, Sidewalk LR doc, smoke 80/80 (o 70/70 con MIOTY pending)
type: project
---

F29 CLOSED 2026-05-03. Branch `feature/f29b-cierre`, tag `v2.0-f29`
(o `v2.0-f29-near-final` si MIOTY excluido). FF'd into `main`.

**8 presets totales en `PROP_PRESETS`** (Python-only, cero cambios firmware):

Vuelta 1 (`v2.0-f29-partial`, commit `04caba9`):
- `sidewalk_915_fsk_50k`, `sidewalk_915_fsk_250k`, `wisun_915_fsk_50k`

Vuelta 2 / F29.b (este cierre):
- `mioty_868_tsunb` — 868 MHz, 396 baud TS-UNB. Status: <PASS si OTA 10/10
  | PENDING si escape M3 activado>
- `wisun_915_fsk_100k`, `_150k`, `_200k`, `_300k` — Wi-SUN FAN 1.0 NA-1 ch 0

**Smoke OTA:** <80/80 si MIOTY OK | 70/70 con MIOTY excluded>. TX board #2
(`/dev/ttyACM1`), RX board #1 (`/dev/ttyACM2`). Antena CatSniffer cubre 868
y 915 MHz sin issue.

**3 demos stub** en `python/examples/lab/`:
- `demo_wisun_scan.py`, `demo_mioty_listen.py`, `demo_sidewalk_subg.py`
- D1 minimalistas: argparse + preset + start_rx + print hex. NO parse de
  stack (PHY-only per `feedback_protocol_vs_phy`).

**Sidewalk LR:** comment inline en `presets.py` documentando que NO se
soporta en CC1352 (requiere SX1262 vía Cat-LoRa).

**Lecciones F29.b:**
- <Documentar qué se aprendió: rx_bw para 300k, MIOTY mod_type ganador,
  cualquier ajuste empírico>

**Why:** Master plan v2.0 §F29 cierre completo. Establece coverage Sub-G
para roadmap v2.0 release.

**How to apply:** Para sniffing/TX en banda Sub-G, usar
`radio.configure_prop(**PROP_PRESETS["<preset_name>"])`. NO sustituye stack
del protocolo — solo provee identidad PHY mínima.

**Out of scope (v2.1):**
- Validación contra hardware real Sidewalk gateway / Wi-SUN node / MIOTY
  base station
- Sync words spec-correct
- Channel hopping plans Wi-SUN FAN
- Sidewalk LR (HW limitation CC1352)
- <Si MIOTY pending: F29.c con SmartRF Studio para custom CPE override>
EOF
```

(Editar el archivo después de crearlo para reemplazar los `<placeholders>` con valores reales según el path tomado.)

- [ ] **Step 4: Actualizar `MEMORY.md` index**

Edit `~/.claude/projects/-home-sabas-Documents-electroniccats-FeralRF/memory/MEMORY.md`. Reemplazar la línea `project_f29_partial`:

```
- [project_f29_partial.md](project_f29_partial.md) — 2026-05-03 tag v2.0-f29-partial. 3 presets Sub-G 915 MHz Sidewalk + Wi-SUN baseline. Smoke 30/30 OTA + CRC. F29.b cubre MIOTY + Wi-SUN FAN completo + spec-correct sync.
```

Por:

```
- [project_f29_partial.md](project_f29_partial.md) — 2026-05-03 v2.0-f29-partial (vuelta 1 histórico, superseded por F29 close)
- [project_f29_done.md](project_f29_done.md) — 2026-05-03 F29 cierre completo. Tag v2.0-f29 (o -near-final si MIOTY excluido). 8 presets Sub-G + demos + Sidewalk LR doc. Smoke 80/80 (o 70/70).
```

- [ ] **Step 5: Actualizar `project_next_session.md`**

Edit el archivo. En la lista "Candidate priorities", reemplazar la entrada F29.b actual:

Buscar el bloque:
```
### 4. F29.b — MIOTY + Wi-SUN FAN completo + Sidewalk fidelidad alta
```

Reemplazar por:
```
### ~~4. F29.b — MIOTY + Wi-SUN FAN completo~~ ✅ CLOSED 2026-05-03
F29 cerrado completo. Tag v2.0-f29 (o v2.0-f29-near-final si MIOTY pending).
Si MIOTY se difirió a v2.1: F29.c sigue como work item — requiere SmartRF
Studio para custom CPE override.
```

Renumerar las entradas siguientes si es necesario (ahora hay un gap).

- [ ] **Step 6: FF merge a `main`**

```bash
cd /home/sabas/Documents/electroniccats/FeralRF
git checkout main
git merge --ff-only feature/f29b-cierre
git update-ref refs/heads/feature/ti-rtos-migration main
git log --oneline -8
```

Expected: FF succeeds (sin merge commit). HEAD = último commit del bundle.

- [ ] **Step 7: Borrar branch local**

```bash
git branch -d feature/f29b-cierre
git branch
```

Expected: branch borrada, ya FF'd.

- [ ] **Step 8: Push (interactivo desde shell del usuario)**

User runs:
```
! git push origin main feature/ti-rtos-migration v2.0-f29
```

(O `v2.0-f29-near-final` si fue path escape.)

NO ejecutar push autónomo per safety rules — esperar confirmación.

---

## Self-review checklist (run before declaring plan complete)

- [ ] **Spec coverage:** spec lista entregables (5 presets, 3 demos, Sidewalk LR doc, smoke 80/80 o 70/70). Plan tiene Task 1 (MIOTY+escape), Task 2 (Wi-SUN+smoke), Task 3 (3 demos), Task 4 (Sidewalk LR doc), Task 5 (tag+memory+FF). ✅
- [ ] **Placeholder scan:** ningún "TBD"/"TODO". Step de escape M3 enumera 4 sub-pasos concretos (M3.1 GFSK, M3.2.a/b/c rx_bw escalation, M3.3 exclude). Step Task 5 step 3 tiene `<DETAILS>` placeholders pero documentados como editables post-fact (no plan failure — son valores que dependen del path). ✅
- [ ] **Type/symbol consistency:** `PROP_PRESETS`, `Radio`, `RxStreamError`, `PHY.PROPRIETARY_GFSK`, `radio.configure_prop`, `radio.set_phy`, `radio.start_rx`, `radio.read_packets`, `pkt.crc_ok`, `pkt.data`, `pkt.rssi_dbm` todos verificados contra HEAD `9d12fd6` (F29 vuelta 1 los validó). Preset names consistentes en presets.py + tests + smoke + demos. ✅
- [ ] **Hardware:** TX board #2 (`/dev/ttyACM1`) / RX board #1 (`/dev/ttyACM2`) heredados de F29 vuelta 1, ambos boards ya flasheados. Pre-flight bisección NO necesaria (vuelta 1 ya validó la cadena). ✅
- [ ] **Compat:** spec dice "cero cambios firmware". Plan no toca nada en `firmware/cc1352/`. ✅
- [ ] **Escape tree:** M3.1 GFSK → M3.2 rx_bw 0x46/0x42/0x4E → M3.3 exclude del smoke + tag near-final. Cada nivel commiteable independiente con mensaje específico. ✅

Si algo falla, fix inline antes del handoff.
