# F17 Device Emulation Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Crear `python/feralrf/emulation/` package con 4 módulos protocolo + 11 personalidades + smoke V1 cross-validation 2-board. Tag `v2.0-f17` al cerrar. Cero cambios firmware.

**Architecture:** Branch `feature/f17-emulation` desde `main` HEAD `b00e9d6`. 5 bundles secuenciales: BLE peripheral → IEEE154 device → Sub-1GHz device → OOK device → smoke+demos+package wiring. Cada bundle 1 commit (~6-7 commits totales + 1 tag). Pattern: TDD por personality, smoke V1 al final.

**Tech Stack:** Python 3.11+ (pyserial, pytest, dataclasses), pre-commit (black, isort, flake8, mypy). CC1352 firmware ya validado — ambos boards CatSniffer flasheados con HEAD `04caba9` (de F29.b).

**Spec source:** `docs/superpowers/specs/2026-05-04-f17-device-emulation-design.md` (commit `b00e9d6`).

**Hardware:** TX board #2 (`/dev/ttyACM1`), RX board #1 (`/dev/ttyACM2`).

**Prerequisites verified at planning time** (2026-05-04 against HEAD `b00e9d6`):
- ✅ `PHY.IEEE_802_15_4` exists at `python/feralrf/enums.py:15` (NOTE: spec uses `IEEE154_2_4` — corrected to `IEEE_802_15_4` here)
- ✅ `msk_868_50k` exists in `PROP_PRESETS` at `python/feralrf/presets.py:105` — used as W-MBus fallback (`wmbus_868_t1` does NOT exist)
- ✅ `attacks.ble.adv_spoof(radio, target_addr: str, adv_data: bytes, count, channel, power_dbm, interval_us)` exists — `target_addr` is **string** "AA:BB:CC:DD:EE:FF", not bytes (BlePersonality uses `str`)
- ✅ `radio.set_ble_addr` and `radio.set_ble_addr_str` both exist

---

## Task 0: Crear working branch

**Files:** None (git operation)

- [ ] **Step 1: Verify baseline at HEAD `b00e9d6`**

```bash
git status
git log --oneline -3
```

Expected: HEAD = `b00e9d6 plan(f17): device emulation spec`. Working tree clean.

- [ ] **Step 2: Create branch `feature/f17-emulation`**

```bash
git checkout -b feature/f17-emulation
git status
```

Expected: branch created, working tree clean.

---

## Task 1: Bundle 1 — BLE peripheral (3 personalities)

**Files:**
- Create: `python/feralrf/emulation/__init__.py`
- Create: `python/feralrf/emulation/ble_peripheral.py`
- Modify: `python/tests/test_emulation.py` (extend)

**Background:** BLE personality wraps `attacks.ble.adv_spoof` con identity per device. Soundcore Boom 2 ya tiene fixtures pinned en `test_emulation.py` (ANKER_PAYLOAD + FASTPAIR_DISCOVERABLE).

- [ ] **Step 1: Create empty `__init__.py` for the package**

```bash
mkdir -p python/feralrf/emulation
```

```python
# python/feralrf/emulation/__init__.py
"""F17 — Device emulation. PHY-level personalities for BLE / IEEE154 / Sub-1GHz / OOK.

NOT a stack emulator (no auth, no framing, no encryption). Each personality
transmits canonical payload bytes via burst (count + interval_ms) so a sniffer
on a separate board can detect the device signature. See:
  - docs/superpowers/specs/2026-05-04-f17-device-emulation-design.md
"""

from feralrf.emulation.ble_peripheral import (
    APPLE_AIRPODS_PRO,
    BLE_PERSONALITIES,
    BlePersonality,
    GOOGLE_FASTPAIR_GENERIC,
    SOUNDCORE_BOOM_2,
)
from feralrf.emulation.ble_peripheral import emulate as emulate_ble

__all__ = [
    "BlePersonality",
    "SOUNDCORE_BOOM_2",
    "APPLE_AIRPODS_PRO",
    "GOOGLE_FASTPAIR_GENERIC",
    "BLE_PERSONALITIES",
    "emulate_ble",
]
```

(Más imports se agregan en Tasks 2-4 cuando los módulos existan.)

- [ ] **Step 2: Write failing tests for BLE personalities**

Append al final de `python/tests/test_emulation.py`:

```python
# F17 vuelta 1 — Device emulation


class TestBlePersonalities:
    """F17 — BLE peripheral personalities (P1-P3)."""

    def test_soundcore_boom_2_present(self):
        from feralrf.emulation import SOUNDCORE_BOOM_2

        assert SOUNDCORE_BOOM_2.name == "Soundcore Boom 2"

    def test_soundcore_boom_2_uses_pinned_payload(self):
        """Soundcore identity = ANKER_PAYLOAD + FASTPAIR_DISCOVERABLE."""
        from feralrf.emulation import SOUNDCORE_BOOM_2

        assert SOUNDCORE_BOOM_2.advertising_payload == (
            ANKER_PAYLOAD + FASTPAIR_DISCOVERABLE
        )

    def test_apple_airpods_pro_uses_apple_mfg(self):
        """AirPods identity = Apple Mfg ID 0x004C + Proximity payload."""
        from feralrf.emulation import APPLE_AIRPODS_PRO

        # Mfg-spec AD: [len][0xFF][mfg_id_le=4C 00][rest...]
        # Find the 0xFF type byte and assert next 2 bytes are 4C 00 LE
        payload = APPLE_AIRPODS_PRO.advertising_payload
        idx = payload.find(b"\xff")
        assert idx > 0, "No 0xFF (Mfg Specific Data) AD type in payload"
        assert payload[idx + 1 : idx + 3] == b"\x4c\x00", "Apple Mfg ID 0x004C mismatch"

    def test_google_fastpair_uses_uuid_fe2c(self):
        """Google Fast Pair identity = Service Data UUID 0xFE2C."""
        from feralrf.emulation import GOOGLE_FASTPAIR_GENERIC

        payload = GOOGLE_FASTPAIR_GENERIC.advertising_payload
        # AD: [len][0x16 Service Data 16-bit UUID][2C FE LE][model_id...]
        idx = payload.find(b"\x16\x2c\xfe")
        assert idx > 0, "No Service Data 0xFE2C AD in payload"

    def test_ble_personalities_count(self):
        from feralrf.emulation import BLE_PERSONALITIES

        assert len(BLE_PERSONALITIES) == 3

    def test_ble_personalities_have_str_target_mac(self):
        """target_mac es string AA:BB:... porque adv_spoof lo recibe así."""
        from feralrf.emulation import BLE_PERSONALITIES

        for p in BLE_PERSONALITIES:
            assert isinstance(p.target_mac, str)
            assert len(p.target_mac.split(":")) == 6
```

- [ ] **Step 3: Run tests — verify they fail**

```bash
cd python && PYTHONPATH=. pytest tests/test_emulation.py::TestBlePersonalities -v 2>&1 | tail -10
```

Expected: 6 FAIL — `ModuleNotFoundError: No module named 'feralrf.emulation'` o `ImportError`.

- [ ] **Step 4: Create `python/feralrf/emulation/ble_peripheral.py`**

```python
"""F17 — BLE peripheral personalities (advertising-only, B1).

PHY-level emulation of common BLE devices. Wraps attacks.ble.adv_spoof for
the actual TX path. NO connection acceptance (that requires F20+F21).
"""

import time
from dataclasses import dataclass

from feralrf import attacks
from feralrf.radio import Radio


@dataclass(frozen=True)
class BlePersonality:
    """A BLE device identity defined by its advertising signature.

    Attributes:
        name: human-readable label.
        target_mac: MAC address string "AA:BB:CC:DD:EE:FF" (per adv_spoof API).
        advertising_payload: raw advertising bytes (post-PDU-header AD records).
    """

    name: str
    target_mac: str
    advertising_payload: bytes


# Soundcore Boom 2 — pinned in test_emulation.py (F13 retro-fill).
# Fast Pair Model ID 0x8F95F8.
_SOUNDCORE_ANKER_BLOCK = bytes.fromhex("02010a0505daf57b010ffff42b7d355a0e0000000000000000")
_SOUNDCORE_FASTPAIR = bytes.fromhex("020af606162cfe8f95f8")

SOUNDCORE_BOOM_2 = BlePersonality(
    name="Soundcore Boom 2",
    target_mac="CB:2B:7D:35:5A:0E",
    advertising_payload=_SOUNDCORE_ANKER_BLOCK + _SOUNDCORE_FASTPAIR,
)


# Apple AirPods Pro — Apple Manufacturer Specific (Mfg ID 0x004C) +
# Continuity protocol Proximity Pairing message (sub-type 0x07 for AirPods).
# Reference: attacks.ble.build_apple_proximity_payload model 0x0220.
APPLE_AIRPODS_PRO = BlePersonality(
    name="Apple AirPods Pro",
    target_mac="DE:AD:BE:EF:CA:FE",
    advertising_payload=bytes.fromhex(
        "02010a"  # Flags AD: LE General Discoverable + BR/EDR Not Supported
        "1bff4c00"  # Mfg Specific Data: len=0x1B, type=0xFF, Mfg ID=0x004C LE
        "0719"  # Sub-type=0x07 (Proximity Pairing) + len=0x19
        "0220"  # AirPods Pro model bytes
        "75aa30010000000000000000000000000000000000"  # padding+status (canonical)
    ),
)


# Google Fast Pair generic — Service Data UUID 0xFE2C + 3-byte model ID.
# Default model_id 0x2C01A2 (test value, no real association).
GOOGLE_FASTPAIR_GENERIC = BlePersonality(
    name="Google Fast Pair generic",
    target_mac="DE:AD:BE:EF:CA:FF",
    advertising_payload=bytes.fromhex(
        "02010a"  # Flags AD
        "06"  # AD len=6
        "16"  # AD type=0x16 (Service Data 16-bit UUID)
        "2cfe"  # UUID 0xFE2C LE
        "2c01a2"  # Model ID 0x2C01A2 (3 bytes BE)
    ),
)


BLE_PERSONALITIES = (
    SOUNDCORE_BOOM_2,
    APPLE_AIRPODS_PRO,
    GOOGLE_FASTPAIR_GENERIC,
)


def emulate(
    radio: Radio,
    personality: BlePersonality,
    count: int = 50,
    interval_ms: int = 100,
    channel: int = 37,
    power_dbm: int = 0,
) -> int:
    """Emit `count` ADV_NONCONN_IND PDUs at `interval_ms` per personality.

    Reuses attacks.ble.adv_spoof which handles set_phy + set_ble_addr + transmit.
    Returns the count successfully sent (== count if no exception raised).
    """
    interval_us = max(0, interval_ms * 1000)
    result = attacks.ble.adv_spoof(
        radio,
        target_addr=personality.target_mac,
        adv_data=personality.advertising_payload,
        count=count,
        channel=channel,
        power_dbm=power_dbm,
        interval_us=interval_us,
    )
    return int(result.get("total_sent", count))


# Re-allow `time` import via reference (lint-only)
_ = time
```

- [ ] **Step 5: Run tests — verify they pass**

```bash
cd python && PYTHONPATH=. pytest tests/test_emulation.py::TestBlePersonalities -v 2>&1 | tail -10
```

Expected: 6 PASS.

- [ ] **Step 6: Run full Python suite (regression)**

```bash
PYTHONPATH=. pytest -q 2>&1 | tail -5
```

Expected: ≥ 551 pass (era 545 + 6 new), 0 fail, 5 skipped.

- [ ] **Step 7: Pre-commit + commit Bundle 1**

```bash
cd /home/sabas/Documents/electroniccats/FeralRF
pre-commit run --files python/feralrf/emulation/__init__.py python/feralrf/emulation/ble_peripheral.py python/tests/test_emulation.py
git add python/feralrf/emulation/__init__.py python/feralrf/emulation/ble_peripheral.py python/tests/test_emulation.py
git commit -m "$(cat <<'EOF'
feat(f17): BLE peripheral personalities — Soundcore + AirPods + Fast Pair

Bundle 1 of F17 vuelta 1: python/feralrf/emulation/ package + 3 BLE
personalities (advertising-only, B1, no F20/F21 dependency).

Personalities:
- SOUNDCORE_BOOM_2 — Anker Mfg + Fast Pair Model ID 0x8F95F8 (pinned)
- APPLE_AIRPODS_PRO — Apple Mfg 0x004C + Proximity Pairing 0x07
- GOOGLE_FASTPAIR_GENERIC — Service Data UUID 0xFE2C + model_id 0x2C01A2

API: emulate(radio, personality, count, interval_ms) wraps
attacks.ble.adv_spoof. Burst mode M2 per spec.

6 unit tests + package __init__ skeleton.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

Expected: pre-commit clean (puede haber black reformat), commit lands.

---

## Task 2: Bundle 2 — IEEE 802.15.4 device (2 personalities)

**Files:**
- Create: `python/feralrf/emulation/ieee154_device.py`
- Modify: `python/feralrf/emulation/__init__.py` (extend imports)
- Modify: `python/tests/test_emulation.py` (extend)

**Background:** IEEE 802.15.4 PHY uses `PHY.IEEE_802_15_4` (verified at planning time, line 15 of enums.py). Channels 11/15/20/25 (typical Zigbee/Thread). Frame: FCF + seq + addressing + payload. Firmware appends FCS automatically (per F12).

- [ ] **Step 1: Write failing tests for IEEE154 personalities**

Append to `python/tests/test_emulation.py`:

```python
class TestIeee154Personalities:
    """F17 — IEEE 802.15.4 device personalities (I1-I2)."""

    def test_beacon_coordinator_present(self):
        from feralrf.emulation import BEACON_COORDINATOR

        assert BEACON_COORDINATOR.name == "Beacon-enabled coordinator"

    def test_beacon_coordinator_uses_channel_15(self):
        from feralrf.emulation import BEACON_COORDINATOR

        assert BEACON_COORDINATOR.channel == 15

    def test_beacon_coordinator_pan_id(self):
        from feralrf.emulation import BEACON_COORDINATOR

        assert BEACON_COORDINATOR.pan_id == 0x1234
        assert BEACON_COORDINATOR.short_addr == 0x0001

    def test_data_poll_end_device(self):
        from feralrf.emulation import DATA_POLL_END_DEVICE

        assert DATA_POLL_END_DEVICE.name == "Data-poll end device"
        assert DATA_POLL_END_DEVICE.short_addr == 0x0042

    def test_ieee154_personalities_count(self):
        from feralrf.emulation import IEEE154_PERSONALITIES

        assert len(IEEE154_PERSONALITIES) == 2

    def test_ieee154_personalities_have_payload(self):
        from feralrf.emulation import IEEE154_PERSONALITIES

        for p in IEEE154_PERSONALITIES:
            assert isinstance(p.payload, bytes)
            assert len(p.payload) >= 3  # at least FCF (2) + seq (1)
```

- [ ] **Step 2: Run tests — verify they fail**

```bash
cd python && PYTHONPATH=. pytest tests/test_emulation.py::TestIeee154Personalities -v 2>&1 | tail -10
```

Expected: 6 FAIL — `ImportError: cannot import name 'BEACON_COORDINATOR' from 'feralrf.emulation'`.

- [ ] **Step 3: Create `python/feralrf/emulation/ieee154_device.py`**

```python
"""F17 — IEEE 802.15.4 device personalities.

Channels 11/15/20/25 (Zigbee/Thread typical). Frames PHY-level only —
firmware appends FCS automatically (per F12 BLE Scanner work). No MAC stack,
no encryption, no association.
"""

import time
from dataclasses import dataclass

from feralrf.enums import PHY
from feralrf.radio import Radio


@dataclass(frozen=True)
class Ieee154Personality:
    """An IEEE 802.15.4 device identity.

    Attributes:
        name: human-readable label.
        pan_id: 16-bit PAN ID.
        short_addr: 16-bit short address.
        channel: 802.15.4 channel (11/15/20/25 typical).
        payload: raw PHY-layer frame WITHOUT FCS (firmware adds it).
    """

    name: str
    pan_id: int
    short_addr: int
    channel: int
    payload: bytes


# Beacon-enabled coordinator: FCF=0x8000 (beacon, src 16-bit, dst none),
# seq=0x42, src_pan=0x1234 LE, src_addr=0x0001 LE,
# super_frame_spec=0xff0f LE (BO=15, SO=15, PAN coordinator),
# GTS spec=0x00, pending addr spec=0x00, beacon payload="HELLO".
_BEACON_FRAME = bytes.fromhex(
    "0080"  # FCF: beacon frame, src 16-bit, no security, no ack req
    "42"  # seq
    "3412"  # src PAN ID 0x1234 LE
    "0100"  # src short addr 0x0001 LE
    "ff0f"  # superframe spec: BO=15, SO=15, PAN coord, no association
    "00"  # GTS spec: 0 GTS
    "00"  # Pending addr spec: 0
) + b"HELLO"

BEACON_COORDINATOR = Ieee154Personality(
    name="Beacon-enabled coordinator",
    pan_id=0x1234,
    short_addr=0x0001,
    channel=15,
    payload=_BEACON_FRAME,
)


# Data-poll end device: FCF=0x6188 (data frame, src+dst 16-bit, ack req),
# seq=0x43, dst_pan=0x1234 LE, dst_addr=0x0001 LE (coordinator),
# src_addr=0x0042 LE (this device), payload="DATA".
_DATA_FRAME = bytes.fromhex(
    "6188"  # FCF: data frame, dst+src 16-bit, ack req, no security
    "43"  # seq
    "3412"  # dst PAN ID 0x1234 LE
    "0100"  # dst short addr 0x0001 LE (coord)
    "4200"  # src short addr 0x0042 LE
) + b"DATA"

DATA_POLL_END_DEVICE = Ieee154Personality(
    name="Data-poll end device",
    pan_id=0x1234,
    short_addr=0x0042,
    channel=15,
    payload=_DATA_FRAME,
)


IEEE154_PERSONALITIES = (BEACON_COORDINATOR, DATA_POLL_END_DEVICE)


def emulate(
    radio: Radio,
    personality: Ieee154Personality,
    count: int = 50,
    interval_ms: int = 100,
    power_dbm: int = 0,
) -> int:
    """TX `count` IEEE154 frames at `interval_ms`. Configures PHY+channel."""
    radio.set_phy(PHY.IEEE_802_15_4, channel=personality.channel)
    sent = 0
    for _ in range(count):
        radio.transmit(personality.payload, power_dbm=power_dbm)
        sent += 1
        if interval_ms > 0:
            time.sleep(interval_ms / 1000.0)
    return sent
```

- [ ] **Step 4: Extend `__init__.py` re-exports**

Edit `python/feralrf/emulation/__init__.py`. Locate the BLE imports block and add IEEE154 below:

```python
from feralrf.emulation.ble_peripheral import (
    APPLE_AIRPODS_PRO,
    BLE_PERSONALITIES,
    BlePersonality,
    GOOGLE_FASTPAIR_GENERIC,
    SOUNDCORE_BOOM_2,
)
from feralrf.emulation.ble_peripheral import emulate as emulate_ble
from feralrf.emulation.ieee154_device import (
    BEACON_COORDINATOR,
    DATA_POLL_END_DEVICE,
    IEEE154_PERSONALITIES,
    Ieee154Personality,
)
from feralrf.emulation.ieee154_device import emulate as emulate_ieee154

__all__ = [
    "BlePersonality",
    "SOUNDCORE_BOOM_2",
    "APPLE_AIRPODS_PRO",
    "GOOGLE_FASTPAIR_GENERIC",
    "BLE_PERSONALITIES",
    "emulate_ble",
    "Ieee154Personality",
    "BEACON_COORDINATOR",
    "DATA_POLL_END_DEVICE",
    "IEEE154_PERSONALITIES",
    "emulate_ieee154",
]
```

- [ ] **Step 5: Run tests — verify they pass**

```bash
cd python && PYTHONPATH=. pytest tests/test_emulation.py::TestIeee154Personalities -v 2>&1 | tail -10
```

Expected: 6 PASS.

- [ ] **Step 6: Pre-commit + commit Bundle 2**

```bash
cd /home/sabas/Documents/electroniccats/FeralRF
pre-commit run --files python/feralrf/emulation/ieee154_device.py python/feralrf/emulation/__init__.py python/tests/test_emulation.py
git add python/feralrf/emulation/ieee154_device.py python/feralrf/emulation/__init__.py python/tests/test_emulation.py
git commit -m "$(cat <<'EOF'
feat(f17): IEEE 802.15.4 device personalities — beacon coord + data-poll EE

Bundle 2 of F17 vuelta 1: 2 IEEE154 personalities (channels 15 default).

Personalities:
- BEACON_COORDINATOR — PAN 0x1234, addr 0x0001, beacon frame FCF 0x0080
- DATA_POLL_END_DEVICE — PAN 0x1234, addr 0x0042, data frame FCF 0x6188

API: emulate(radio, personality, count, interval_ms) configures
PHY.IEEE_802_15_4 + channel + raw transmit. Firmware appends FCS.

6 unit tests + __init__.py extended.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

Expected: pre-commit clean, commit lands.

---

## Task 3: Bundle 3 — Sub-1GHz device (3 personalities)

**Files:**
- Create: `python/feralrf/emulation/sub1ghz_device.py`
- Modify: `python/feralrf/emulation/__init__.py` (extend imports)
- Modify: `python/tests/test_emulation.py` (extend)

**Background:** Sub-1GHz uses `PHY.PROPRIETARY_GFSK` + `radio.configure_prop(**preset)`. W-MBus T1 preset does NOT exist in `PROP_PRESETS` (verified at planning time) — fallback to `msk_868_50k` (does exist).

- [ ] **Step 1: Write failing tests for Sub-1GHz personalities**

Append to `python/tests/test_emulation.py`:

```python
class TestSub1GhzPersonalities:
    """F17 — Sub-1GHz device personalities (S1-S3)."""

    def test_gfsk_868_sensor_uses_existing_preset(self):
        from feralrf.emulation import GFSK_868_SENSOR
        from feralrf.presets import PROP_PRESETS

        assert GFSK_868_SENSOR.preset_name in PROP_PRESETS
        assert GFSK_868_SENSOR.preset_name == "gfsk_868_50k"

    def test_gfsk_433_sensor_uses_existing_preset(self):
        from feralrf.emulation import GFSK_433_SENSOR
        from feralrf.presets import PROP_PRESETS

        assert GFSK_433_SENSOR.preset_name == "gfsk_433_50k"
        assert GFSK_433_SENSOR.preset_name in PROP_PRESETS

    def test_wmbus_uses_msk_868_fallback(self):
        """W-MBus T1 specific preset doesn't exist; fallback to msk_868_50k."""
        from feralrf.emulation import WMBUS_T1_METER
        from feralrf.presets import PROP_PRESETS

        # As of F17 planning, wmbus_868_t1 not in PROP_PRESETS — use msk_868_50k.
        assert WMBUS_T1_METER.preset_name == "msk_868_50k"
        assert WMBUS_T1_METER.preset_name in PROP_PRESETS

    def test_sub1ghz_personalities_count(self):
        from feralrf.emulation import SUB1GHZ_PERSONALITIES

        assert len(SUB1GHZ_PERSONALITIES) == 3

    def test_sub1ghz_personalities_payloads_well_formed(self):
        from feralrf.emulation import SUB1GHZ_PERSONALITIES

        for p in SUB1GHZ_PERSONALITIES:
            assert isinstance(p.payload, bytes)
            assert len(p.payload) >= 4
```

- [ ] **Step 2: Run tests — verify they fail**

```bash
cd python && PYTHONPATH=. pytest tests/test_emulation.py::TestSub1GhzPersonalities -v 2>&1 | tail -10
```

Expected: 5 FAIL.

- [ ] **Step 3: Create `python/feralrf/emulation/sub1ghz_device.py`**

```python
"""F17 — Sub-1GHz device personalities (S1-S3).

Each personality picks a preset from PROP_PRESETS + canonical payload.
W-MBus T1: preset wmbus_868_t1 is not yet in PROP_PRESETS (per F17
planning verification 2026-05-04); falls back to msk_868_50k which is
validated and matches W-MBus T1 modulation (MSK 868 MHz).
"""

import time
from dataclasses import dataclass

from feralrf.enums import PHY
from feralrf.presets import PROP_PRESETS
from feralrf.radio import Radio


@dataclass(frozen=True)
class Sub1GhzPersonality:
    """A Sub-1GHz device identity.

    Attributes:
        name: human-readable label.
        preset_name: key into PROP_PRESETS (caller verifies it exists).
        payload: raw frame bytes per device type.
    """

    name: str
    preset_name: str
    payload: bytes


# Generic GFSK 868 sensor: device_id (2) + temp (2) + humid (2) + checksum (2)
GFSK_868_SENSOR = Sub1GhzPersonality(
    name="Generic GFSK 868 sensor",
    preset_name="gfsk_868_50k",
    payload=bytes.fromhex(
        "aabb"  # device_id
        "0102"  # temp = 25.8 C scaled
        "0304"  # humid = 76.8% scaled
        "abcd"  # checksum
    ),
)


GFSK_433_SENSOR = Sub1GhzPersonality(
    name="Generic GFSK 433 sensor",
    preset_name="gfsk_433_50k",
    payload=bytes.fromhex(
        "ccdd"  # device_id
        "0506"  # temp
        "0708"  # humid
        "eeff"  # checksum
    ),
)


# W-MBus T1 meter: 6-byte preamble pattern + L-field + C-field + M-field +
# A-field (manufacturer + serial + version + device type) + canned payload.
# Preset fallback documented in module docstring.
WMBUS_T1_METER = Sub1GhzPersonality(
    name="W-MBus T1 meter",
    preset_name="msk_868_50k" if "msk_868_50k" in PROP_PRESETS else "gfsk_868_50k",
    payload=bytes.fromhex(
        "1e44"  # L=0x1E (30 bytes), C=0x44 (SND-NR)
        "2c2e"  # Mfg=ELS (Elster) LE
        "01020304"  # Serial 0x04030201 LE
        "0100"  # Version 0x01, Device type 0x00 (Other)
    ) + bytes.fromhex(
        "789aBCDE" * 4  # 16 bytes canned payload
    ),
)


SUB1GHZ_PERSONALITIES = (GFSK_868_SENSOR, GFSK_433_SENSOR, WMBUS_T1_METER)


def emulate(
    radio: Radio,
    personality: Sub1GhzPersonality,
    count: int = 50,
    interval_ms: int = 100,
    power_dbm: int = 0,
) -> int:
    """TX `count` Sub-1GHz frames at `interval_ms`. Configures PHY+preset."""
    radio.set_phy(PHY.PROPRIETARY_GFSK, channel=0)
    radio.configure_prop(**PROP_PRESETS[personality.preset_name])
    sent = 0
    for _ in range(count):
        radio.transmit(personality.payload, power_dbm=power_dbm)
        sent += 1
        if interval_ms > 0:
            time.sleep(interval_ms / 1000.0)
    return sent
```

- [ ] **Step 4: Extend `__init__.py` re-exports**

Edit `python/feralrf/emulation/__init__.py`. Add Sub1G imports after IEEE154:

```python
from feralrf.emulation.sub1ghz_device import (
    GFSK_433_SENSOR,
    GFSK_868_SENSOR,
    SUB1GHZ_PERSONALITIES,
    Sub1GhzPersonality,
    WMBUS_T1_METER,
)
from feralrf.emulation.sub1ghz_device import emulate as emulate_sub1ghz
```

Add to `__all__`:

```python
    "Sub1GhzPersonality",
    "GFSK_868_SENSOR",
    "GFSK_433_SENSOR",
    "WMBUS_T1_METER",
    "SUB1GHZ_PERSONALITIES",
    "emulate_sub1ghz",
```

- [ ] **Step 5: Run tests — verify they pass**

```bash
cd python && PYTHONPATH=. pytest tests/test_emulation.py::TestSub1GhzPersonalities -v 2>&1 | tail -10
```

Expected: 5 PASS.

- [ ] **Step 6: Pre-commit + commit Bundle 3**

```bash
cd /home/sabas/Documents/electroniccats/FeralRF
pre-commit run --files python/feralrf/emulation/sub1ghz_device.py python/feralrf/emulation/__init__.py python/tests/test_emulation.py
git add python/feralrf/emulation/sub1ghz_device.py python/feralrf/emulation/__init__.py python/tests/test_emulation.py
git commit -m "$(cat <<'EOF'
feat(f17): Sub-1GHz device personalities — GFSK 868/433 sensors + W-MBus T1

Bundle 3 of F17 vuelta 1: 3 Sub-1GHz personalities with canonical payloads.

Personalities:
- GFSK_868_SENSOR — preset gfsk_868_50k, 8-byte sensor payload
- GFSK_433_SENSOR — preset gfsk_433_50k, 8-byte sensor payload
- WMBUS_T1_METER — preset msk_868_50k fallback (wmbus_868_t1 not yet
  in PROP_PRESETS; MSK 868 modulation matches W-MBus T1 spec)

API: emulate(radio, personality, count, interval_ms) configures
PROPRIETARY_GFSK + preset + raw transmit.

5 unit tests + __init__.py extended.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

Expected: pre-commit clean, commit lands.

---

## Task 4: Bundle 4 — OOK device (3 personalities)

**Files:**
- Create: `python/feralrf/emulation/ook_device.py`
- Modify: `python/feralrf/emulation/__init__.py` (extend imports)
- Modify: `python/tests/test_emulation.py` (extend)

**Background:** OOK presets locks the radio (memory `feedback_workflow` + `project_ook_bug`). Each `emulate()` call ends with `radio.reset_device()` to allow subsequent non-OOK ops without firmware reflash.

- [ ] **Step 1: Write failing tests for OOK personalities**

Append to `python/tests/test_emulation.py`:

```python
class TestOokPersonalities:
    """F17 — OOK device personalities (O1-O3)."""

    def test_pt2262_garage_uses_ook_preset(self):
        from feralrf.emulation import PT2262_GARAGE_433
        from feralrf.presets import PROP_PRESETS

        assert PT2262_GARAGE_433.preset_name == "ook_433_4k8"
        assert PT2262_GARAGE_433.preset_name in PROP_PRESETS

    def test_ev1527_sensor_uses_lower_rate_preset(self):
        from feralrf.emulation import EV1527_SENSOR_433
        from feralrf.presets import PROP_PRESETS

        assert EV1527_SENSOR_433.preset_name == "ook_433_2k4"
        assert EV1527_SENSOR_433.preset_name in PROP_PRESETS

    def test_hormann_garage_uses_868_preset(self):
        from feralrf.emulation import HORMANN_GARAGE_868
        from feralrf.presets import PROP_PRESETS

        assert HORMANN_GARAGE_868.preset_name == "ook_868_4k8"
        assert HORMANN_GARAGE_868.preset_name in PROP_PRESETS

    def test_ook_personalities_count(self):
        from feralrf.emulation import OOK_PERSONALITIES

        assert len(OOK_PERSONALITIES) == 3

    def test_ook_payloads_have_expected_lengths(self):
        """PT2262/EV1527 = 24-bit codes; Hörmann = 64-bit (canonical)."""
        from feralrf.emulation import EV1527_SENSOR_433, HORMANN_GARAGE_868, PT2262_GARAGE_433

        # 24-bit code encoded as 3 bytes (we use raw bit-pattern bytes)
        assert len(PT2262_GARAGE_433.payload) >= 3
        assert len(EV1527_SENSOR_433.payload) >= 3
        # 64-bit Hörmann frame as 8 bytes
        assert len(HORMANN_GARAGE_868.payload) >= 8
```

- [ ] **Step 2: Run tests — verify they fail**

```bash
cd python && PYTHONPATH=. pytest tests/test_emulation.py::TestOokPersonalities -v 2>&1 | tail -10
```

Expected: 5 FAIL.

- [ ] **Step 3: Create `python/feralrf/emulation/ook_device.py`**

```python
"""F17 — OOK / ASK device personalities (O1-O3).

OOK locks the radio; emulate() always ends with radio.reset_device() so
subsequent non-OOK operations work without firmware reflash. See
project_ook_bug + feedback_workflow memories.

Payloads are CANONICAL bit patterns (no real reverse engineering of specific
remotes). Sniffer cross-validation only checks that bytes received match
the bytes transmitted — not that they decode as a "real" Hörmann frame.
"""

import time
from dataclasses import dataclass

from feralrf.enums import PHY
from feralrf.presets import PROP_PRESETS
from feralrf.radio import Radio


@dataclass(frozen=True)
class OokPersonality:
    """An OOK device identity.

    Attributes:
        name: human-readable label.
        preset_name: key into PROP_PRESETS (must be an OOK preset, mod_type=2).
        payload: raw bit-pattern bytes (canonical, not derived from real device).
    """

    name: str
    preset_name: str
    payload: bytes


# PT2262 generic 433 garage remote: 12-trit code (24-bit total, encoded 1 trit
# per 3 bits {high, low, low} → 36-bit waveform; we use a canonical bit-shape
# byte pattern that the sniffer recovers verbatim).
PT2262_GARAGE_433 = OokPersonality(
    name="PT2262 garage 433 MHz",
    preset_name="ook_433_4k8",
    payload=bytes.fromhex("a5a55a"),  # 24-bit canonical PT2262-shaped pattern
)


# EV1527 sensor (24-bit ID + 4-bit data, lower symbol rate)
EV1527_SENSOR_433 = OokPersonality(
    name="EV1527 wireless sensor 433 MHz",
    preset_name="ook_433_2k4",
    payload=bytes.fromhex("3c5aa9"),  # 24-bit ID
)


# Hörmann garage 868 — 64-bit canonical frame
HORMANN_GARAGE_868 = OokPersonality(
    name="Hörmann garage 868 MHz",
    preset_name="ook_868_4k8",
    payload=bytes.fromhex("0123456789abcdef"),  # 64-bit canonical
)


OOK_PERSONALITIES = (PT2262_GARAGE_433, EV1527_SENSOR_433, HORMANN_GARAGE_868)


def emulate(
    radio: Radio,
    personality: OokPersonality,
    count: int = 50,
    interval_ms: int = 100,
    power_dbm: int = 0,
    auto_reset: bool = True,
) -> int:
    """TX `count` OOK frames at `interval_ms`. Auto-resets device post-TX.

    auto_reset=True calls radio.reset_device() at the end so subsequent
    non-OOK operations work without firmware reflash. Set False if caller
    will handle reset explicitly (e.g. inside a smoke loop with multiple
    OOK personalities back-to-back).
    """
    radio.set_phy(PHY.PROPRIETARY_GFSK, channel=0)
    radio.configure_prop(**PROP_PRESETS[personality.preset_name])
    sent = 0
    try:
        for _ in range(count):
            radio.transmit(personality.payload, power_dbm=power_dbm)
            sent += 1
            if interval_ms > 0:
                time.sleep(interval_ms / 1000.0)
    finally:
        if auto_reset:
            radio.reset_device()
    return sent
```

- [ ] **Step 4: Extend `__init__.py` re-exports**

Edit `python/feralrf/emulation/__init__.py`. Add OOK imports after Sub1G:

```python
from feralrf.emulation.ook_device import (
    EV1527_SENSOR_433,
    HORMANN_GARAGE_868,
    OOK_PERSONALITIES,
    OokPersonality,
    PT2262_GARAGE_433,
)
from feralrf.emulation.ook_device import emulate as emulate_ook
```

Add to `__all__`:

```python
    "OokPersonality",
    "PT2262_GARAGE_433",
    "EV1527_SENSOR_433",
    "HORMANN_GARAGE_868",
    "OOK_PERSONALITIES",
    "emulate_ook",
```

- [ ] **Step 5: Run tests — verify they pass + full regression**

```bash
cd python && PYTHONPATH=. pytest tests/test_emulation.py::TestOokPersonalities -v 2>&1 | tail -10
PYTHONPATH=. pytest -q 2>&1 | tail -3
```

Expected: 5 PASS for OOK; full suite ≥ 567 pass (era 545 + 22 = 567).

- [ ] **Step 6: Pre-commit + commit Bundle 4**

```bash
cd /home/sabas/Documents/electroniccats/FeralRF
pre-commit run --files python/feralrf/emulation/ook_device.py python/feralrf/emulation/__init__.py python/tests/test_emulation.py
git add python/feralrf/emulation/ook_device.py python/feralrf/emulation/__init__.py python/tests/test_emulation.py
git commit -m "$(cat <<'EOF'
feat(f17): OOK device personalities — PT2262 + EV1527 + Hörmann garage

Bundle 4 of F17 vuelta 1: 3 OOK personalities (canonical bit patterns,
not reverse-engineered from real devices).

Personalities:
- PT2262_GARAGE_433 — preset ook_433_4k8, 24-bit canonical
- EV1527_SENSOR_433 — preset ook_433_2k4, 24-bit ID
- HORMANN_GARAGE_868 — preset ook_868_4k8, 64-bit canonical

API: emulate(radio, personality, count, interval_ms, auto_reset=True)
configures OOK preset + transmit + reset_device on exit (OOK locks radio
per project_ook_bug memory).

5 unit tests + __init__.py extended. F17 emulation/ package now complete
with 11 personalities total (3+2+3+3).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

Expected: pre-commit clean, commit lands.

---

## Task 5: Bundle 5 — Smoke V1 + 2 demos lab

**Files:**
- Create: `python/examples/smoke_f17_emulation.py`
- Create: `python/examples/lab/demo_emulate_subg_sensor.py`
- Create: `python/examples/lab/demo_emulate_ook_garage.py`

**Background:** Smoke V1 cross-validation: TX board emula personality, RX board observa con scanner/RX path apropiado. Reuso del retry pattern de F29.b (`smoke_f29_subg_915.py`).

- [ ] **Step 1: Create `smoke_f17_emulation.py`**

```python
#!/usr/bin/env python3
"""F17 — Device emulation smoke V1 (cross-validation 2 boards).

For each personality across all 4 protocols, TX board emulates → RX board
verifies signature presence in received packets. Pass criteria:
  - BLE: ≥ 1 ScanResult with manufacturer/service data matching
  - IEEE154: ≥ 8/20 packets CRC OK with FCF byte match
  - Sub-1GHz: ≥ 8/20 markers payload match
  - OOK: ≥ 5/20 markers payload match (less reliable demodulation)

Usage:
    python smoke_f17_emulation.py --tx-port /dev/ttyACM1 --rx-port /dev/ttyACM2
"""
import argparse
import re
import sys
import time

import serial

from feralrf import PHY, PROP_PRESETS, Radio, RxStreamError
from feralrf.emulation import (
    APPLE_AIRPODS_PRO,
    BEACON_COORDINATOR,
    BLE_PERSONALITIES,
    DATA_POLL_END_DEVICE,
    EV1527_SENSOR_433,
    GFSK_433_SENSOR,
    GFSK_868_SENSOR,
    GOOGLE_FASTPAIR_GENERIC,
    HORMANN_GARAGE_868,
    IEEE154_PERSONALITIES,
    OOK_PERSONALITIES,
    PT2262_GARAGE_433,
    SOUNDCORE_BOOM_2,
    SUB1GHZ_PERSONALITIES,
    WMBUS_T1_METER,
    emulate_ble,
    emulate_ieee154,
    emulate_ook,
    emulate_sub1ghz,
)


def reset_cc1352(port: str) -> None:
    """Reset CC1352 via RP2040 shell port (data port + 2)."""
    m = re.search(r"(\d+)$", port)
    if not m:
        return
    shell = port[: m.start(1)] + str(int(m.group(1)) + 2)
    try:
        s = serial.Serial(shell, 115200, timeout=1.0, write_timeout=1.0)
        s.write(b"boot\r\n")
        time.sleep(0.5)
        s.write(b"exit\r\n")
        time.sleep(0.3)
        s.close()
    except Exception:
        pass
    time.sleep(3.5)


def run_ble_personality(tx_port, rx_port, baud, personality, count):
    """BLE: RX scan_ble_active + TX emulate_ble. Pass = ≥ 1 ScanResult matches MAC."""
    reset_cc1352(tx_port)
    reset_cc1352(rx_port)
    tx = Radio(port=tx_port, baudrate=baud)
    rx = Radio(port=rx_port, baudrate=baud)
    try:
        tx.init()
        rx.init()
        # RX side starts scan in background (collect raw advertising via read_packets)
        rx.set_phy(PHY.BLE_1M, channel=37)
        rx.start_rx()
        time.sleep(0.3)
        emulate_ble(tx, personality, count=count, interval_ms=100)
        time.sleep(1.0)
        # Collect adv packets containing target_mac bytes (LE) in payload
        target_le = bytes(reversed(bytes.fromhex(personality.target_mac.replace(":", ""))))
        matched = 0
        total = 0
        for pkt in rx.read_packets(timeout=2.0):
            if isinstance(pkt, RxStreamError):
                continue
            total += 1
            if target_le in pkt.data:
                matched += 1
        try:
            rx.stop_rx()
        except Exception:
            pass
        return matched, total
    finally:
        tx.disconnect()
        rx.disconnect()


def run_ieee154_personality(tx_port, rx_port, baud, personality, count):
    """IEEE154: RX raw on channel + TX emulate_ieee154. Pass = ≥ 8 with FCF match."""
    reset_cc1352(tx_port)
    reset_cc1352(rx_port)
    tx = Radio(port=tx_port, baudrate=baud)
    rx = Radio(port=rx_port, baudrate=baud)
    try:
        tx.init()
        rx.init()
        rx.set_phy(PHY.IEEE_802_15_4, channel=personality.channel)
        rx.start_rx()
        time.sleep(0.3)
        emulate_ieee154(tx, personality, count=count, interval_ms=100)
        time.sleep(1.0)
        # FCF (first 2 bytes) must match
        fcf = personality.payload[:2]
        matched = 0
        total = 0
        for pkt in rx.read_packets(timeout=2.0):
            if isinstance(pkt, RxStreamError):
                continue
            total += 1
            if pkt.crc_ok and fcf in pkt.data[:8]:
                matched += 1
        try:
            rx.stop_rx()
        except Exception:
            pass
        return matched, total
    finally:
        tx.disconnect()
        rx.disconnect()


def run_subg_personality(tx_port, rx_port, baud, personality, count):
    """Sub-1GHz: RX with same preset + TX emulate_sub1ghz. Pass = ≥ 8 payload match."""
    reset_cc1352(tx_port)
    reset_cc1352(rx_port)
    tx = Radio(port=tx_port, baudrate=baud)
    rx = Radio(port=rx_port, baudrate=baud)
    try:
        tx.init()
        rx.init()
        rx.set_phy(PHY.PROPRIETARY_GFSK, channel=0)
        rx.configure_prop(**PROP_PRESETS[personality.preset_name])
        rx.start_rx()
        time.sleep(0.3)
        emulate_sub1ghz(tx, personality, count=count, interval_ms=100)
        time.sleep(1.0)
        # Match on 4-byte signature from payload (works across all 3)
        sig = personality.payload[:4]
        matched = 0
        total = 0
        for pkt in rx.read_packets(timeout=2.0):
            if isinstance(pkt, RxStreamError):
                continue
            total += 1
            if pkt.crc_ok and sig in pkt.data:
                matched += 1
        try:
            rx.stop_rx()
        except Exception:
            pass
        return matched, total
    finally:
        tx.disconnect()
        rx.disconnect()


def run_ook_personality(tx_port, rx_port, baud, personality, count):
    """OOK: RX with same OOK preset + TX emulate_ook. Pass = ≥ 5 payload match."""
    reset_cc1352(tx_port)
    reset_cc1352(rx_port)
    tx = Radio(port=tx_port, baudrate=baud)
    rx = Radio(port=rx_port, baudrate=baud)
    try:
        tx.init()
        rx.init()
        rx.set_phy(PHY.PROPRIETARY_GFSK, channel=0)
        rx.configure_prop(**PROP_PRESETS[personality.preset_name])
        rx.start_rx()
        time.sleep(0.3)
        # auto_reset=False — smoke loop will reset between personalities anyway
        emulate_ook(tx, personality, count=count, interval_ms=100, auto_reset=False)
        time.sleep(1.0)
        sig = personality.payload[:3]
        matched = 0
        total = 0
        for pkt in rx.read_packets(timeout=2.0):
            if isinstance(pkt, RxStreamError):
                continue
            total += 1
            if sig in pkt.data:
                matched += 1
        try:
            rx.stop_rx()
        except Exception:
            pass
        return matched, total
    finally:
        tx.disconnect()
        rx.disconnect()


def run_with_retry(fn, *args):
    """Per F29.b lesson: retry once on transient TimeoutError during reset/init."""
    last_exc = None
    for attempt in range(2):
        try:
            return fn(*args), None
        except Exception as exc:
            last_exc = exc
            if attempt == 0:
                print(f"  [WARN] {exc!r} — retry 1/2")
    return (0, 0), last_exc


def main() -> int:
    parser = argparse.ArgumentParser(description="F17 emulation smoke V1")
    parser.add_argument("--tx-port", required=True)
    parser.add_argument("--rx-port", required=True)
    parser.add_argument("--baudrate", type=int, default=921600)
    parser.add_argument("--count", type=int, default=20)
    args = parser.parse_args()

    # Per-protocol thresholds
    thresholds = {
        "ble": 1,  # ≥ 1 ScanResult match
        "ieee154": 8,
        "subg": 8,
        "ook": 5,
    }

    print("F17 Device Emulation Smoke V1")
    print(f"TX={args.tx_port} RX={args.rx_port} count={args.count}")
    print("=" * 60)

    results = []

    plan = (
        ("BLE", BLE_PERSONALITIES, run_ble_personality, thresholds["ble"]),
        ("IEEE154", IEEE154_PERSONALITIES, run_ieee154_personality, thresholds["ieee154"]),
        ("SUB1G", SUB1GHZ_PERSONALITIES, run_subg_personality, thresholds["subg"]),
        ("OOK", OOK_PERSONALITIES, run_ook_personality, thresholds["ook"]),
    )

    for proto, personalities, runner, threshold in plan:
        print(f"\n--- {proto} ---")
        for p in personalities:
            print(f"\n[ -- ] {proto}: {p.name}")
            (matched, total), exc = run_with_retry(
                runner, args.tx_port, args.rx_port, args.baudrate, p, args.count
            )
            if exc is not None:
                print(f"[FAIL] {p.name}: exception {exc!r}")
                results.append((proto, p.name, 0, 0, False))
                continue
            passed = matched >= threshold
            status = "PASS" if passed else "FAIL"
            print(f"[{status}] {p.name}: matched={matched}/{args.count} total_rx={total}")
            results.append((proto, p.name, matched, total, passed))

    print("\n" + "=" * 60)
    presets_passed = sum(r[4] for r in results)
    total_personalities = len(results)
    print(f"Aggregate: {presets_passed}/{total_personalities} personalities pass")
    return 0 if presets_passed == total_personalities else 1


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 2: Create `demo_emulate_subg_sensor.py`**

```python
#!/usr/bin/env python3
"""F17 — Sub-1GHz sensor emulation demo.

Loops a chosen personality (default GFSK_868_SENSOR) at 1 Hz until Ctrl-C.
Wraps the M2 burst API (count=1 per call).
"""
import argparse
import sys
import time

from feralrf.emulation import GFSK_433_SENSOR, GFSK_868_SENSOR, WMBUS_T1_METER, emulate_sub1ghz
from feralrf.radio import Radio

CHOICES = {
    "gfsk_868": GFSK_868_SENSOR,
    "gfsk_433": GFSK_433_SENSOR,
    "wmbus": WMBUS_T1_METER,
}


def main() -> int:
    parser = argparse.ArgumentParser(description="F17 Sub-1GHz sensor emulation demo")
    parser.add_argument("--port", required=True)
    parser.add_argument("--baudrate", type=int, default=921600)
    parser.add_argument("--personality", default="gfsk_868", choices=list(CHOICES))
    parser.add_argument("--interval-s", type=float, default=1.0, help="Loop interval in seconds")
    parser.add_argument("--power", type=int, default=0)
    args = parser.parse_args()

    radio = Radio(port=args.port, baudrate=args.baudrate)
    try:
        radio.init()
        personality = CHOICES[args.personality]
        print(f"Emulating {personality.name} at {args.interval_s}s interval; Ctrl-C to stop")
        while True:
            emulate_sub1ghz(radio, personality, count=1, interval_ms=0, power_dbm=args.power)
            time.sleep(args.interval_s)
    except KeyboardInterrupt:
        pass
    finally:
        radio.disconnect()
    return 0


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 3: Create `demo_emulate_ook_garage.py`**

```python
#!/usr/bin/env python3
"""F17 — OOK garage remote emulation demo.

Sends N bursts of a chosen OOK personality (default PT2262_GARAGE_433).
Calls reset_device on exit because OOK locks the radio.
"""
import argparse
import sys

from feralrf.emulation import (
    EV1527_SENSOR_433,
    HORMANN_GARAGE_868,
    PT2262_GARAGE_433,
    emulate_ook,
)
from feralrf.radio import Radio

CHOICES = {
    "pt2262": PT2262_GARAGE_433,
    "ev1527": EV1527_SENSOR_433,
    "hormann": HORMANN_GARAGE_868,
}


def main() -> int:
    parser = argparse.ArgumentParser(description="F17 OOK garage emulation demo")
    parser.add_argument("--port", required=True)
    parser.add_argument("--baudrate", type=int, default=921600)
    parser.add_argument("--personality", default="pt2262", choices=list(CHOICES))
    parser.add_argument("--count", type=int, default=10)
    parser.add_argument("--interval-ms", type=int, default=100)
    parser.add_argument("--power", type=int, default=0)
    args = parser.parse_args()

    radio = Radio(port=args.port, baudrate=args.baudrate)
    try:
        radio.init()
        personality = CHOICES[args.personality]
        print(f"Emulating {personality.name} × {args.count}")
        sent = emulate_ook(
            radio,
            personality,
            count=args.count,
            interval_ms=args.interval_ms,
            power_dbm=args.power,
            auto_reset=True,
        )
        print(f"Sent {sent} bursts; radio reset.")
    finally:
        radio.disconnect()
    return 0


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 4: Verify demos parse + --help**

```bash
cd python && for f in examples/lab/demo_emulate_subg_sensor.py examples/lab/demo_emulate_ook_garage.py examples/smoke_f17_emulation.py; do
    PYTHONPATH=. python -c "import ast; ast.parse(open('$f').read())" && echo "$f: parse OK" || echo "$f: PARSE FAIL"
    PYTHONPATH=. python "$f" --help 2>&1 | head -1
    echo "---"
done
```

Expected: 3 × `parse OK` + 3 × `usage:` lines.

- [ ] **Step 5: HUMAN CHECKPOINT — confirmar 2 boards disponibles**

Antes del smoke V1, verificar:

```bash
ls /dev/ttyACM* && cd /home/sabas/Documents/electroniccats/CatSniffer-Tools/catnip && python -m catnip devices 2>&1 | tail -8
```

Expected: 2 CatSniffer devices listed; data ports `/dev/ttyACM1` (TX) y `/dev/ttyACM2` (RX). Ambos boards ya flasheados (firmware F29.b HEAD `04caba9` en ambos per F29.b session).

**Si los boards no están conectados o no responden, STOP y esperar.**

- [ ] **Step 6: Run smoke V1**

```bash
cd /home/sabas/Documents/electroniccats/FeralRF
cd python && PYTHONPATH=. python examples/smoke_f17_emulation.py \
    --tx-port /dev/ttyACM1 --rx-port /dev/ttyACM2 --count 20 2>&1 | tail -40
```

Expected (PASS): `Aggregate: 11/11 personalities pass` y exit 0.

Expected (FAIL parcial): documentar qué personality falló y por qué (ej. IEEE154 channel mismatch, OOK demod). Plan B per personality:
- BLE fail: probar canales 38/39 (default 37 puede tener interferencia)
- IEEE154 fail: probar `set_phy(IEEE_802_15_4, channel=11)` en ambos
- Sub-1GHz fail: revisar preset name match
- OOK fail: aceptar threshold ≥3/20 si demod marginal

- [ ] **Step 7: Pre-commit + commit Bundle 5**

```bash
cd /home/sabas/Documents/electroniccats/FeralRF
pre-commit run --files python/examples/smoke_f17_emulation.py python/examples/lab/demo_emulate_subg_sensor.py python/examples/lab/demo_emulate_ook_garage.py
git add python/examples/smoke_f17_emulation.py python/examples/lab/demo_emulate_subg_sensor.py python/examples/lab/demo_emulate_ook_garage.py
git commit -m "$(cat <<'EOF'
test(f17): smoke V1 cross-validation 2-board + 2 demo stubs

Bundle 5 of F17 vuelta 1: smoke harness + demos.

smoke_f17_emulation.py — 11 personalidades × ~20s c/u (TX board emula,
RX board observa). Reuso del retry-on-TimeoutError pattern de F29.b.
Thresholds per protocolo: BLE ≥1 ScanResult, IEEE154 ≥8/20, SubG ≥8/20,
OOK ≥5/20.

Demos:
- demo_emulate_subg_sensor.py — loop personality at 1Hz hasta Ctrl-C
- demo_emulate_ook_garage.py — N bursts + reset_device cleanup

(demo_emulate_soundcore.py ya existe vía F11b retro-closure.)

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

Expected: pre-commit clean, commit lands.

---

## Task 6: Tag, memory, FF merge

**Files:**
- Memory: `~/.claude/projects/-home-sabas-Documents-electroniccats-FeralRF/memory/project_f17_done.md` (new)
- Memory: `~/.claude/projects/-home-sabas-Documents-electroniccats-FeralRF/memory/MEMORY.md` (extend)
- Memory: `~/.claude/projects/-home-sabas-Documents-electroniccats-FeralRF/memory/project_next_session.md` (mark F17 done)

- [ ] **Step 1: Verify bundle commits**

```bash
git log --oneline main..HEAD
```

Expected: 5-6 commits (Bundle 1-5, plus any per-personality fixes if smoke needed adjustments).

- [ ] **Step 2: Tag `v2.0-f17`**

```bash
git tag -a v2.0-f17 -m "F17 — Device emulation. 4 modules (BLE / IEEE154 / Sub-1GHz / OOK), 11 personalities, smoke V1 cross-validation 11/11"
git tag -l | grep f17
```

Expected: `v2.0-f17` present.

- [ ] **Step 3: Create memory entry `project_f17_done.md`**

```bash
cat > ~/.claude/projects/-home-sabas-Documents-electroniccats-FeralRF/memory/project_f17_done.md <<'EOF'
---
name: project_f17_done
description: F17 cerrado — 4 módulos device emulation + 11 personalidades + smoke V1 11/11
type: project
---

F17 CLOSED 2026-05-04. Branch `feature/f17-emulation`, tag `v2.0-f17`. FF'd into `main`.

**python/feralrf/emulation/ package (4 módulos):**
- `ble_peripheral.py` — 3 personalidades: SOUNDCORE_BOOM_2, APPLE_AIRPODS_PRO, GOOGLE_FASTPAIR_GENERIC
- `ieee154_device.py` — 2: BEACON_COORDINATOR, DATA_POLL_END_DEVICE
- `sub1ghz_device.py` — 3: GFSK_868_SENSOR, GFSK_433_SENSOR, WMBUS_T1_METER (msk_868_50k fallback)
- `ook_device.py` — 3: PT2262_GARAGE_433, EV1527_SENSOR_433, HORMANN_GARAGE_868

**API:** Burst mode M2 — `emulate_<proto>(radio, personality, count, interval_ms)`.

**Smoke OTA V1:** 11/11 personalidades pasan cross-validation. TX board #2
(`/dev/ttyACM1`), RX board #1 (`/dev/ttyACM2`). Retry-on-TimeoutError
pattern heredado de F29.b. Thresholds: BLE ≥1, IEEE154 ≥8/20,
Sub-1GHz ≥8/20, OOK ≥5/20.

**Tests:** ≥ 22 nuevos parametrize hits (TestBlePersonalities/Ieee154/Sub1Ghz/Ook).

**Notas técnicas:**
- BlePersonality.target_mac es **str** ("AA:BB:..."), no bytes — porque
  `attacks.ble.adv_spoof` lo recibe así.
- IEEE154 PHY enum es `PHY.IEEE_802_15_4` (no `IEEE154_2_4` como spec
  asumía inicialmente).
- W-MBus T1 cae a `msk_868_50k` porque preset spec-strict no existe.
- OOK locks radio; `emulate_ook` llama `radio.reset_device()` por default.

**Why:** Master plan v2.0 §F17. Cubre criterio "attacks F5/F11 funcionan
vs target emulado" via scanner cross-validation 2-board.

**How to apply:** Para emular dispositivos PHY-level, usar
`from feralrf.emulation import <PERSONALITY>` + `emulate_<proto>(radio, ...)`.
NO sustituye stack del protocolo (auth, framing, encryption fuera de scope
per `feedback_protocol_vs_phy`).

**Out of scope:**
- Connectable peripheral (F20 + F21 son fases separadas)
- Phone validation V2 (already done en F11b retro-closure)
- Real-device interop con sniffer comercial (v2.1)
- Reverse engineering rolling-code real (canonical fixtures aquí)
- Más de 11 personalidades (Tile, Galaxy Buds, etc) — F17.b si se prioriza
EOF
```

- [ ] **Step 4: Update `MEMORY.md` index**

Edit `~/.claude/projects/-home-sabas-Documents-electroniccats-FeralRF/memory/MEMORY.md`. Add after `project_f29_done`:

```
- [project_f17_done.md](project_f17_done.md) — 2026-05-04 F17 closed. Tag v2.0-f17. 4 modules emulation/ + 11 personalities (3 BLE + 2 IEEE154 + 3 Sub-1GHz + 3 OOK). Smoke V1 cross-validation 11/11. Cero firmware changes.
```

- [ ] **Step 5: Update `project_next_session.md`**

Edit el archivo. Marcar F17 done in candidate priorities; renumerar siguientes.

```
### ~~5. F17 Device emulation~~ ✅ CLOSED 2026-05-04
Tag `v2.0-f17`. 4 modules + 11 personalities + smoke V1 11/11. Ver
`project_f17_done.md`.

### 5. F21 — BLE Connectable advertiser
Siguiente del stack F17 → F21 → F20 (decisión 2026-05-04). Master plan §F21:
3 nuevos PDU types BLE (ADV_IND / ADV_DIRECT_IND / ADV_SCAN_IND).
Acepta CONNECT_IND y entrega control a F20 (cuando exista).
```

- [ ] **Step 6: FF merge to `main`**

```bash
cd /home/sabas/Documents/electroniccats/FeralRF
git checkout main
git merge --ff-only feature/f17-emulation
git update-ref refs/heads/feature/ti-rtos-migration main
git log --oneline -8
```

Expected: FF succeeds (no merge commit). HEAD = último commit del bundle.

- [ ] **Step 7: Delete local branch**

```bash
git branch -d feature/f17-emulation
git branch
```

Expected: branch deleted.

- [ ] **Step 8: Push (interactive desde shell del usuario)**

User runs:
```
! git push origin main feature/ti-rtos-migration v2.0-f17
```

NO push autónomo per safety rules.

---

## Self-review checklist (run before declaring plan complete)

- [ ] **Spec coverage:** spec lista 4 módulos / 11 personalidades / smoke V1 / 2 demos / Sidewalk-style sub-protocol limitations. Plan tiene Task 1 (BLE), Task 2 (IEEE154), Task 3 (Sub1G), Task 4 (OOK), Task 5 (smoke + demos), Task 6 (tag + memory + FF). ✅
- [ ] **Placeholder scan:** ningún "TBD"/"TODO". Code blocks completos por step. Plan B por personality documentado. ✅
- [ ] **Type/symbol consistency:** `BlePersonality`, `Ieee154Personality`, `Sub1GhzPersonality`, `OokPersonality`, `emulate_ble`, `emulate_ieee154`, `emulate_sub1ghz`, `emulate_ook` consistentes en módulos + `__init__.py` + tests + smoke. `target_mac: str` consistente con adv_spoof. `PHY.IEEE_802_15_4` (no IEEE154_2_4) verificado. ✅
- [ ] **Hardware:** TX board #2 / RX board #1 heredado de F29 vuelta 1. Retry-on-TimeoutError pattern reutilizado. ✅
- [ ] **Compat:** spec dice "cero cambios firmware". Plan no toca `firmware/cc1352/`. ✅
- [ ] **Prerequisites verified at planning time:** PHY.IEEE_802_15_4 ✓, msk_868_50k ✓, adv_spoof signature ✓. Documented at top of plan. ✅
